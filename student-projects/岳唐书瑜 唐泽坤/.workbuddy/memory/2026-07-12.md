# 2026-07-12 工作日志

## 恐龙背单词 v4 优化
- **恐龙SVG全部改为侧面视图**：蛋(地面阴影+裂纹)、幼年(侧面朝右+大眼+背刺+尾巴+肚皮)、成年(侧面T-Rex风格+粗壮腿+尖牙+背刺排+小手臂)、死亡(灰度侧面+X眼+灵魂)
- **恐龙移到导航栏**：标签改为"恐龙成长"，首页去掉宠物卡片只留学习卡，宠物详情弹窗加大SVG尺寸
- **考核解除所有限制**：dailyReset只判定hasWords，Quiz.start不检查lastQuizDate/quizAvailable，complete不锁定。只要有词就能考，不限次数
- **背完弹窗加"去测试"按钮**：complete弹窗新增🧪去测试昨日单词按钮
- **Pet.update()安全化**：所有DOM操作加null检查，首页元素移除后不报错
- **错题本只从考核录入**：学习阶段Learn.next()不碰错题本，只有Quiz.answer()答错才录入

## 恐龙背单词 v5 完整重置
- **系统全面清零**：STORAGE_KEY改为`dino_words_v5`，Store.load()重写为只保留DEFAULT_DATA中已知字段，清除所有旧数据
- **恐龙图片AI化**：用ImageGen生成4张可爱卡通T-Rex图片(蛋/幼年/成年/死亡)，替换掉所有SVG代码，DINO_IMG对象引用PNG文件，Pet.update()用`<img>`标签渲染
- **学习无限制**：Learn.init()当todayLearned>=dailyGoal时自动清零开新批次，新增Learn.startNewBatch()方法，首页学习按钮永远enabled
- **考核无限制**：Quiz.start()只检查有无可考单词，不检查日期/锁定状态；Quiz.complete()不设置lastQuizDate；DEFAULT_DATA删除quizAvailable和lastQuizDate字段；dailyReset删除quizAvailable逻辑
- **庆祝弹窗三按钮**：继续学习新词 / 去测试 / 返回首页
- **Learn.complete()归档逻辑**：yesterdayBank使用Set去重合并，不再覆盖
- **清理临时文件**：fix_dino.py已删除
- **验证完成**：所有代码段检查无误，4张PNG图片就位，文件结构完整(1587行)

## 修复JS语法错误
- **问题**：Learn.complete()方法后缺少逗号，导致`startNewBatch()`无法解析，整个JS崩溃，页面所有功能失效(按钮打不开、暗色模式切换不了)
- **修复**：在complete()的`}`后加逗号 → `},`
- **验证**：用Node.js `new Function(js)` 语法检查通过，38479字符JS无错误
- **教训**：批量替换代码后必须做JS语法校验，对象字面量中方法之间必须有逗号分隔

## 添加背诵完成动画
- **全屏完成动画覆盖层**：背完每日目标后触发，z-index:10000
- **浅色模式=撒花**：花瓣(椭圆)+彩纸(矩形)从顶部掉落，马卡龙配色，左右摇摆+旋转
- **深色模式=烟花**：火箭从底部升空→到达随机高度爆炸→粒子向外扩散+重力下落+渐隐，带发光效果(shadowBlur)
- **动画内容**：🎉emoji弹跳 + "你已背诵完成"大字 + 完成单词数副标题 + 成长值奖励 + "返回主页"按钮
- **Learn.complete()改为调用CompletionAnim.show()**，不再用旧的celebrate弹窗
- **旧celebrate弹窗简化**：只保留"返回首页"按钮，仅供宠物复活场景使用
- **Canvas动画引擎**：requestAnimationFrame循环，粒子系统，自动清理离屏粒子

## 首页重构 + 导航标签化
- **导航栏重构**：品牌名改为"Dino Words"，增加三个标签页：首页/每日单词/记忆考核，标签有active高亮状态(渐变背景)
- **首页改为欢迎页**：恐龙图片(浮动动画+阴影脉动) + 渐变色标题"恐龙背单词" + 副标题 + 统计卡片(今日进度/已学单词/等级/连续打卡) + 快捷按钮
- **漂浮单词背景**：15个英文单词(apple/study/happy等)在背景中缓慢上浮+旋转，马卡龙配色，低透明度
- **Pages.show()更新**：切换页面时自动更新nav-tab的active状态
- **init()改为Pages.show('home')**：默认进入欢迎页而非学习页
- **refreshHome()更新**：渲染欢迎页恐龙图片、连续打卡数
- **右侧工具栏精简**：错题本带文字、恐龙/设置/主题只留图标

## 日期打卡系统 + 进化地图
- **数据模型新增**：`todayCheckedIn`(布尔)标记今日是否已打卡，`dayRecords`数组记录每日打卡详情(日期/天数/单词数/单词列表/错题列表)
- **每日定量打卡**：背完dailyGoal个单词才算一次打卡，`Learn.complete()`首次完成时设todayCheckedIn=true、streak++、记录dayRecord；额外学习不再增加打卡天数
- **每日重置**：`dailyReset()`新一天重置todayCheckedIn=false
- **额外学习模式**：已打卡后继续背单词显示橙色提示横幅，进度条显示"额外学习 · 第N词"，完成后弹toast"本批额外学习完成"
- **恐龙动画优化**：去掉drop-shadow边框，动画从简单dinoFloat改为4段dinoBounce(上下弹跳+旋转+缩放)，更活泼
- **首页重构**：去掉统计卡片和快捷按钮(已在导航栏)，保留恐龙蛋+标题，下方新增"恐龙进化之路"地图
- **洪恩识字风格地图**：蛇形排列的圆形节点(每行5个，奇偶行反向)，已完成=绿蓝渐变+Day N标签+可点击，今天=橙黄脉动+🎯，未打卡=灰色锁定+🔒；节点间有连线，已完成连线为渐变色
- **日期详情弹窗**：点击已完成节点弹出当日详情：日期/背诵单词数/当日单词列表(英-中)/当日错题列表(粉红标记)
- **DayMap模块**：render()根据dayRecords渲染地图，showDetail(idx)展示当日详情
- **startNewBatch安全化**：不再重置todayLearned(保留打卡记录)
- **dayRecords day字段**：使用dayRecords.length+1而非streak(避免断签后天数重复)
- **13处代码变更**通过Python脚本批量应用，JS语法验证通过
