# 2026-07-12 工作日志

## 摸金行动 2D 射击游戏（三角洲风格）
- 交付单文件 HTML 游戏：`delta-loot-2d.html`（960x540 canvas，打开即玩）。
- 玩法：横版搜刮撤离。A/D 移动，鼠标左右决定朝向，左键射击、右键跳跃；撞障碍时自动连点左键两次破障（已修复射击冷却导致第二发被吞的 bug）。
- 功能键各 1 个：Enter 开始 / P 暂停 / R 复位 / F 拾取。
- 有 AI 人机：从两侧刷出，追击、跳跃越障、水平射击妨碍玩家；可击杀掉落；可破坏木箱后露出金。
- 目标：搜刮满 150 金激活右侧撤离点并走到绿灯处撤离；HP 归零失败。
- 注意：用户口语中有错别字（"嗯""按月"），按功能意图实现即可。

## 迭代：首页 + 角色外观升级
- 重做开始菜单（首页）：动态粒子背景、英雄+双敌人角色展示、半透明操作面板（移动/射击/跳跃/破障/拾取 + Enter/P/R/F 四个功能键）、闪烁「按 Enter 开始」。
- 角色从方块改为 `drawHumanoid` 战术小人：头盔、护目镜、战术背心、背包（仅玩家）、持枪姿势、走动时腿部摆动；敌人受击白闪。
- 帧计数移到主循环，首页也能动起来；敌人新增 facing 朝向。
- 已通过 JS 语法检查。

## 迭代：鼠标控制前后移动
- 移动改为鼠标驱动：鼠标在角色右侧→向右(前)走并朝右，左侧→向左(后)走并朝左；鼠标居中→停下且保留上次朝向（死区 14px）。朝向与射击方向统一跟随鼠标。
- A/D、← → 保留为备用移动键。
- 同步更新底部帮助条与首页操作面板文案。
- 已通过 JS 语法检查。

## 迭代：收集物改为大红历史卡片
- 把金（金）收集物替换为「大红历史卡片」：红底金边、写「史」字；共 8 张，藏在地面/箱顶/平台/可破坏木箱后。
- 8 张卡各对应一个不同历史故事（司母戊鼎、兵马俑、丝绸之路、活字印刷、郑和下西洋、都江堰、甲骨文、京杭大运河），拾取时弹出故事卡（drawCardStory + wrapText，约 4.3 秒）。
- 过关条件改为：集齐 5 张历史卡片即过关（state='win'），移除原 150 金撤离逻辑；右侧绿门改作装饰「终点」。
- HUD/首页/帮助条同步改为卡片进度与提示。
- 已通过 JS 语法检查，确认无 TARGET_SCORE/game.score/lootCount 残留引用。

## 迭代：开始键改为右键三连击
- 开始/重开触发由 Enter 键改为「鼠标右键三连击」：600ms 内记录右键点击时间，满 3 次调用 onStart()（仅在 menu/win/over 生效，游戏中不影响右键跳跃）；触发后清除 mouse.right 避免开局误跳。
- 去掉 keydown 里的 Enter→onStart；首页面板、闪烁提示、底部帮助条、win/over 文案同步改为「右键三连击 开始」。
- 已通过 JS 语法检查，确认无 Enter 残留引用。

## 迭代：历史卡片故事字体放大（两次）
- 第1次：正文 14→19px（≈5mm），标题 16→22px，弹窗 440x116→470x132，行高 20→26。
- 第2次（再次放大）：正文 19→26px，标题 22→30px，弹窗 470x132→580x164，行高 26→34。
- 已通过 JS 语法检查。

## 迭代：首页「历史之树」元进度系统
- 新增跨局持久化的 `treeMeta.fragments`，存于 localStorage 键 `mjj_tree`（try/catch 容错，file:// 下失败则仅内存）。
- 规则：每集齐 5 张历史卡片过关 → +1 树枝碎片；每 4 碎片 → 点亮 1 棵树（cap 8）。
- 首页新增 `drawHistoryTree(cx, baseY, fragments)`：树干+土堆+8 个树冠，点亮的发绿光并带光晕；左侧显示「树枝碎片 / 已点亮树木 / 距下一棵」。英雄居中被双敌人护卫，操作面板移至右侧。
- 游戏内 HUD 加「🌟 树枝碎片」行；过关弹窗显示「获得树枝碎片×1（共 N）」。
- 关于用户问「用不用我来拿给我」：碎片为过关自动发放，无需玩家手动获取；进度自动保存。
- 已通过 JS 语法检查，确认 drawHistoryTree/treeMeta/saveMeta/mjj_tree 均已接好。

## 迭代：新增「历史花园」界面（G 键切换）
- 新增状态 'garden'；按 G 在 菜单/过关/失败 ↔ 花园 间切换，花园内按 G 或 Esc 返回首页（游戏进行中按 G 无效，避免误丢进度）。
- `drawGarden()`：草地+太阳背景，按 lit=floor(碎片/4) 在网格（8列×最多3行，cap 24）种下小树 `drawGardenTree`，显示「已种下树木 N 棵 / 树枝碎片」，空花园与超额有提示；底部闪烁「按 G 或 Esc 返回首页」。
- 首页加「按 G 进入历史花园」提示；底部帮助条加 `G 花园`；过关弹窗加「按 G 看花园」。
- 已通过 JS 语法检查，确认 drawGarden/toggleGarden/state==='garden'/KeyG 均已接好。

## 迭代：恐怖风改造（血色骷髅 + 红色死亡动画）
- 新增 `drawSkull(cx,cy,s,headColor,eyeColor)`：颅骨+下颌+牙缝+眼窝+发光眼+鼻洞，眼睛带 shadowBlur 红光。
- 首页开始动画：底部中央血红色骷髅（headColor #7a0d12 / eye #ff2b2b），带脉动血色光晕、轻微漂浮；原「英雄+双敌护卫」角色展示移除。标题改为血红(#ff5a5a)+红晕，副标题「恐怖风格」。
- 死亡动画 `drawDeath()`（state==='over' 调用）：血红径向背景 + 大骷髅(#8b0a12/#ff1a1a) + 脉动血色暗角 + 「你 已 被 历 史 吞 噬」+ 重来/花园提示。
- 游戏内恐怖氛围：世界背景渐变改深红黑(#1c0e12→#0a0507)、远景#1a1014，游戏中叠加红色暗角 vignette。
- 已通过 JS 语法检查，确认 drawSkull/drawDeath/over 分支/配色均已接好。

## 迭代：成功动画改为「撤离成功」绿色主题
- 新增 `drawWin()`（state==='win' 调用）：与红色死亡动画对称的绿色版——深绿径向背景 + 发光绿骷髅(#0e5a2e/#39ff9a, shadowBlur 绿) + 脉动绿色暗角 + 「撤 离 成 功」+ 卡片/击杀/碎片/重来提示。
- win 状态由通用 `overlay('过关！历史卡片集齐',...)` 改为 `{ drawWin(); return; }`。
- 全局文案「过关」统一为「撤离成功」：底部帮助条、HUD 撤离提示、首页目标、花园空提示、注释。
- 已通过 JS 语法检查，确认 drawWin/win 分支均接好。

## 迭代：首页角色/武器选择区 + 角色专属技能
- 新增常量 `CHARACTERS`（4 角色：突击兵/医疗兵/侦察兵/重装兵，含 body/dark/skin/helmet 配色、hp、maxv、skill 名、skillDesc、skillCd）与 `WEAPONS`（手枪/步枪/霰弹枪/狙击枪，含 cooldown/speed/dmg/pellets/spread/color/bulletR）。
- 选择状态 `selChar/selWeapon`，持久化于 localStorage `mjj_sel`；首页点击切换（menuButtons 记录矩形，mousedown 左键调 handleMenuClick）。
- 首页重写为两列布局：左列历史之树+角色选择区（4 卡，含技能名/说明），右列操作面板+武器选择区（4 卡，含伤害/弹丸/射速）。
- newGame 应用角色配色/HP/速度到 player（body/dark/skin/helmet/maxv/hp）；playerShoot 用 WEAPONS 参数（射速/伤害/弹丸数/竖直散布/子弹色，子弹带 dmg 字段命中时 e.hp-=b.dmg）。
- 技能系统 `useSkill()`（Q 键）：手雷轰炸(范围炸伤)/战地急救(回40HP)/疾风冲刺(1.5s加速+二段跳)/能量护盾(2s无敌)；update 递减 skillTimer/dashTimer/shieldTimer，冲刺期 maxv*1.7 且可二段跳，护盾期免疫敌人子弹伤害；玩家绘制用所选配色并加护盾光环。
- HUD 新增「角色 X · 武器 Y · Q[技能] 就绪/冷却」行；底部帮助条加 Q 技能；操作面板加 Q 行。
- 已通过 JS 语法检查，确认 CHARACTERS/WEAPONS/selChar/selWeapon/menuButtons/useSkill/handleMenuClick 全部接好。

## 迭代：改为平面游戏 + 降低难度
- 平面化：移除高箱(70高不可破坏)与平台(130高)，障碍仅剩 2 个地面可破坏木箱(46高, hp4)位于 x=420/720 挡住其后卡片；8 张历史卡片全部落地(GROUND_Y)并重新排在屏幕内 x∈[180,835]（W=960, 终点门在890）；远景剪影改矮(GROUND_Y-80,高80)增强平面感。
- 降难度：敌人 HP 3→2、移速 1.7→1.15、开枪冷却 60+40→95+60、射击距离 380→300；在场上限 5→3、刷怪周期 200→320、开局刷怪延迟 120→160；敌人子弹伤害 12→8、受击无敌帧 30→45。
- 已通过 JS 语法检查，确认各改动均接好。

## 迭代：新增「飞行兵」敌人类型
- spawnEnemy 约 35% 概率生成飞行兵(type:'fly')：空中悬停追击，baseY=GROUND_Y-60~82 正弦浮动(±16)；hp=1/maxhp=1（一枪死）；从空中朝玩家斜射(enemyShoot 角度瞄准，距离<340 开火，cd 90~140)。地面兵补 maxhp=2。
- updateEnemy 开头按 type 分流：飞行兵走 updateFlying（不重力/不 collide/不跳，仅水平缓移+浮动+射击）；地面兵逻辑不变。
- 敌人绘制分支：飞行兵用紫色特战配色(#9b59d0/#5e337a)+煽动翅膀(ellipse, sin flap)+地面影子；血条统一改 e.hp/e.maxhp。
- 已通过 JS 语法检查，确认 type:'fly'/updateFlying/绘制分支/血条均接好。

## 迭代：胜利条件改为「走到终点才算」
- newGame 中 exActive 初始改为 false；集齐 5 张卡不再直接 win，而是 game.exActive=true 并提示「终点已激活」。
- update 末尾新增判定：仅当 exActive 且玩家中心到达 exX(x=W-70) 且在地面 → state='win'（绿色撤离成功动画）。
- HUD 顶部提示跟随 exActive：未激活显示「集齐N张卡激活终点（已 x/5）」，激活后显示「✔ 终点已激活 — 向右走到终点胜利！」。
- 文案同步：帮助条、首页目标提示均改为「集齐5张卡激活终点，走到终点才胜利」。
- 已通过 JS 语法检查。

## 迭代：击杀人机掉落历史卡片直接进背包
- 玩家子弹击杀敌人（e.hp<=0）时，50% 概率使 game.cardCount++（直接入账，无需按 F 拾取）；集齐5张时同 tryPickup 触发碎片+激活终点。
- 掉落提示 toast：普通「💀 击杀掉落历史卡片！背包 n/5」，集齐时「💀 击杀掉落卡片！集齐5张·获树枝碎片×1·终点已激活」。
- 底部帮助条加说明「击杀人机也有 50% 概率直接掉落历史卡片进背包（无需拾取）」。
- 已通过 JS 语法检查。

## 迭代：新增「冷知识卡片」类型
- 新增 TRIVIA 数组（8 条历史/文物冷知识）。卡片分两类：大红=历史故事卡、青蓝=冷知识卡（写「冷」字）。
- 地图 8 张卡交错为 4 历史 + 4 冷知识，胜利条件仍为集齐任意 5 张（cardCount>=5）激活终点；必须至少捡 1 张冷知识卡才能凑满 5 张。
- 卡片带 kind 字段；绘制按 kind 着色；拾取弹窗 drawCardStory 按 kind 用红/青配色（历史📜 / 冷知识💡）。
- HUD 显示「卡片 n/5 (史 x · 冷 y)」；击杀掉落随机 history/trivia 并同步细分计数。
- 帮助条/首页目标/胜负界面文案同步改为「史/冷」双类型。
- 已通过 JS 语法检查。

## 迭代：胜利动画改为「乘直升机撤离」
- drawWin 由绿色骷髅改为：夜空渐变 + 月亮 + 闪烁星星 + 地面剪影背景；直升机悬停（sin 浮动）、旋翼旋转（rotate frame*0.6，半透明旋翼盘）、尾梁/尾翼/起落架/座舱玻璃/打开的舱门；玩家用 drawHumanoid 站在打开的舱门口（蓝色特战配色）。文字保留「撤 离 成 功」并加前缀「乘直升机撤离」。
- 已通过 JS 语法检查，确认 drawWin 重写成功。

## 迭代：生成角色/武器图片并全面科技化
- 用 ImageGen 生成 8 张透明底 1024x1024 科幻图：4 角色立绘（突击/医疗/侦察/重装，暗色装甲+青色/绿色/橙色霓虹）+ 4 武器图（手枪/步枪/霰弹/狙击，黑铁+霓虹光）。图片保存到 assets/ 目录。
- HTML 中新增 CHAR_SPRITES / WP_SPRITES / SPRITE_FILES / IMAGES / loadSprites() / isImageReady()，new Image() 预加载。
- 新增 drawSpriteEntity(e, key, opts)：加载成功时按贴图绘制，支持左右翻转（scale -1）、走动 bob、source-atop 红色染色、受击白闪；失败时 fallback 到 drawHumanoid。
- 玩家绘制改用当前角色贴图（drawSpriteEntity），枪口位置从 p.x+p.w/2 改为 p.x+p.w/2 + facing*19 / p.y+18；敌人（地面+飞行）统一用突击兵贴图+红色染色，飞行兵保留翅膀/影子。
- 首页角色卡、武器卡分别绘制对应图片缩略图；选中卡片加霓虹高亮和四角角标。
- 科技感：世界背景改深紫黑渐变，叠加青色科技网格和动态扫描线；地面加红/绿霓虹边；HUD 左侧加半透明面板与四角角标；首页操作面板/历史树/角色卡/武器卡加科幻边框与角标；标题下加红色扫描线。
- 已通过 JS 语法检查。

## 迭代：每局随机战斗场地
- newGame 内关卡布局改为程序随机生成：木箱数量 1~3（随机 x∈[220,780]，彼此间距≥130）；8 张卡片（4 史+4 冷交错，i 偶历史奇冷）随机 x∈[160,845] 互不重叠（间距≥72），其中 min(2,boxes) 张随机藏到木箱右后(x=bx+48)，需打掉木箱才能拿（保留破障玩法）。场地仍保持单一平面、全部在地面、不超界(W=960,终点890)。无旧 obs 引用残留。
- 已通过 JS 语法检查。

## 迭代：4 棵历史树兑换新武器
- 需求：用户要求「四棵树可以兑换一把新武器」。
- 实现（按字面：4 棵点亮的历史树 = 16 树枝碎片 兑换 1 把隐藏武器）：
  - WEAPONS 拆为 BASE_WEAPONS(4 把开局可选) + BONUS_WEAPONS(3 把隐藏科技武器：等离子炮/电磁轨道炮/量子湮灭枪)，WEAPONS = 两者 concat。
  - treeMeta 增加 weapons:[]（持久化）；weaponUnlocked(i) 判断可选择性；未解锁武器在武器区显示 🔒 覆盖层。
  - 首页历史树面板下方新增「兑换新武器(4树)」按钮（type:'exchange'，lit≥4 且未全解锁时可点），点击 doExchange() 扣 16 碎片、解锁下一把、saveMeta、弹提示。
  - 武器区改为 4 列动态网格（≤4 单行 / 5~7 两行自适应），新增 drawWeaponCard / drawWeaponIcon(无贴图时的矢量枪械图标) 辅助函数。
  - 选武器点击加未解锁拦截提示；localStorage 读取后若 selWeapon 指向未解锁武器则回退为 0。
  - 首页操作面板提示、底部提示、帮助面板、花园「已用 X 树兑换武器」统计同步更新。
- 注意：兑换会消耗 4 棵树（花园相应少 4 棵，已说明）。若用户其实指「4 树枝碎片」而非「4 棵树」，需改 doExchange 扣 4 而非 16。已通过 JS 语法检查。

## 迭代：武器商城（金币货币购买）
- 需求：用户要求「设置武器商城」。
- 实现：新增金币货币体系（treeMeta.coins 持久化）：
  - 赚金币：击杀 +8、拾卡 +15、撤离成功 +80（addCoins 写入并 saveMeta）；HUD 显示「💰 金币 X（本局+…）」。
  - BONUS_WEAPONS 每把加 price 字段（激光步枪300/等离子炮450/电磁轨道炮600/量子湮灭枪800/火箭筒1000）。
  - 新增 state='shop' 全屏商城 + drawShop()：3 列卡片网格，显示武器图标/属性/价格，按钮分「✓已装备/点击装备/💰购买/不足」；buyWeapon(i) 扣金币解锁并自动装备；toggleShop()/handleShopClick()；B 键进入，B/Esc 返回。
  - 首页历史树面板下方并排「兑换(4树)」+「🛒商城」按钮；帮助面板、底部提示、武器区锁定提示同步更新。
- 已通过 JS 语法检查。

## 迭代：大地图（相机横向滚动）+ 图片放大
- 需求：用户要求「图做的大一点儿，地图做的大一点儿」。
- 实现：
  - 地图变大：新增 WORLD_W = 2600（屏幕 W=960），相机 camX 横向跟随玩家（computeCam()：居中玩家并 clamp 到 [0, WORLD_W-W]）。
  - newGame 把木箱/卡片/终点(exX=WORLD_W-70) 铺满整张大地图（EDGE=160 留白，间隔拉大）。
  - collide() 右边界改 WORLD_W；spawnEnemy 改为玩家屏幕边缘外刷新（±W/2+40，clamp 到世界内）；子弹剔除用 WORLD_W。
  - 鼠标前后移动改用玩家屏幕坐标：pcx = p.x + p.w/2 - camX（update 里先 camX=computeCam()）。
  - drawField 拆为 drawSky(屏幕)/drawGround(屏幕)/drawScenery(世界，铺满 WORLD_W，随相机滚动)；draw() 对世界实体整体 ctx.save();translate(-camX,0);…;ctx.restore()，HUD/暗角/弹窗保持屏幕空间。
  - HUD 底部新增地图位置进度条（玩家横坐标占 WORLD_W 比例 + 终点标记）。
  - 图片放大：drawSpriteEntity 默认尺寸 54x70 → 76x104；玩家护盾圈半径 30 → 46；首页角色卡片缩略图 ih 86→102 并下移文字排版；武器区高度 260→268。
- 已通过 JS 语法检查（node --check）。

## 迭代：商城改为纯金币数目兑换（移除 4 树兑换）
- 需求：用户要求「将商城中的东西为数目兑换」——商城内武器统一用金币数目购买，不再用「4 棵历史树」兑换。
- 实现：
  - 删除 doExchange() / firstLockedBonus() 及首页「兑换(4树)」按钮（type:'exchange'）；把原并排按钮简化为整行「🛒 武器商城(金币兑换)」按钮。
  - 移除 handleMenuClick 的 exchange 分支；武器区锁定提示改为「🔒 商城金币解锁 / 💰 X 金币」；选武器拦截提示改为「去🛒武器商城(B)用金币数目兑换」。
  - 帮助面板/操作面板/底栏/花园「已用 X 树兑换武器」等树兑换相关文字全部清除；历史树现仅用于花园展示。
  - 商城购买逻辑 buyWeapon()（wp.price + treeMeta.coins）保持不变，纯金币数目兑换。
- 已通过 JS 语法检查。

## 迭代：每局最终 BOSS
- 需求：用户要求「每局设置一个最终boss」。
- 实现：
  - 常量 BOSS_NAMES（5 个随机名）；newGame 新增 game.boss=null / bossSpawned=false / bossDead=false / bossName='' / bossArenaX=WORLD_W-760。
  - 玩家进入 bossArenaX 范围即 spawnBoss()：x=exX-200、w64h96、hp50、用 heavy 贴图放大+深红染、暗红威压光环+地面阴影，三连发散射 bossShoot()，updateBoss() 缓慢逼近(±0.8)、地面 clamp、接触伤害14。
  - 子弹命中 boss（player bullets 循环内新增 boss 判定）：hp<=0 → bossDead=true、爆裂粒子、+120 金币、提示「前往终点撤离」。
  - 胜利条件增加 game.bossDead 前置；BOSS 战期间停止刷小怪（spawnEnemy 被 bossSpawned 抑制）。
  - HUD 顶部居中大血条（game.boss.name + hp/maxhp）；右侧提示改为 BOSS 未击败时红字「先击败 BOSS」；帮助面板/底栏加 BOSS 提示。
- 已通过 JS 语法检查。

## 迭代：战斗场景三主题（城市沼泽 / 沼泽 / 森林）
- 需求：用户要求「战斗场景为城市沼、沼泽、森林」。
- 实现：
  - 新增 FIELDS 常量（3 主题：city 城市沼泽 / swamp 沼泽 / forest 森林，含中文 name）。
  - newGame 中随机选 game.field = FIELDS[随机下标]（每局不同）。
  - 新增 drawField(field) 替换原固定科技背景：按主题绘制天空渐变 + 中景（城市=半淹废墟高楼+霓虹倒影；沼泽=枯树+雾气；森林=树干树冠+阳光束）+ 地面与边缘色。
  - draw() 中仅战斗相关状态（playing/paused/win/over）绘制具体主题，其余界面（menu/shop/garden）自绘整屏背景。
  - HUD 顶部居中新增「⚔ X战场」战场名标签（game.field.name）。
- 已通过 JS 语法检查。

## 迭代：武器酷炫特效
- 需求：用户要求「给我的武器设计更多酷炫的特效」。
- 实现：
  - 新增 weaponFx(wp)：按武器名返回特效类型（tracer/tracer/spread/beam/laser/plasma/rail/quantum/rocket）。
  - playerShoot 给每发子弹打上 fx 标签；新增玩家 muzzle 计时（开火置 7）+ muzzleColor/muzzleFx。
  - 新增 drawBulletFx(b)：按 fx 绘制——laser/beam/rail 为拉长高亮光束+白亮芯（rail 附电弧）；plasma 脉动等离子球+外环；quantum 紫能球+环绕粒子；rocket 弹体+尾焰；tracer/spread 发光弹丸+拖尾。均用 shadowBlur 辉光。
  - 玩家绘制块新增枪口闪光（径向渐变光球+朝向星芒，随 muzzle 衰减）。
  - 敌人子弹加红色辉光；命中敌人火花改用子弹本色 b.color。
  - update 中新增 p.muzzle 递减。
- 已通过 JS 语法检查。

## 重大迭代：2D 横版 → 3D 第一视角（摸金行动 3D）
- 需求：用户要求「将它变成3D的第一视角，摸金游戏」。
- 交付新文件：`delta-loot-3d.html`（单文件，无外部依赖，复用 assets/ 下 8 张 PNG 角色/武器图）。
- 技术选型：Wolfenstein 风格 raycasting（DDA 栅格射线 + 每列 z-buffer Float32Array ZB），不用 WebGL/Three.js 以保证离线单文件可玩。
- 关卡：MAP_W=21, MAP_H=96 深廊道栅格；genMap 生成边界墙 + 随机掩体 + 中央保证通道；player 在 (cx, 2.2) 朝 +Z 深度方向。含 8 张卡、2-4 木箱、终点门、末端 boss。
- 操作：WASD 移动 + Pointer Lock 鼠标转视角（movementX * 0.0022）；十字准星；第一人称武器视图（drawViewModel + 后坐力 + 枪口闪光）。点击 canvas 锁定鼠标。
- 射击：hitscan 沿角度 ray-march（步长 0.2）命中墙/箱/敌/boss，含 LOS 判定 + 曳光弹；FBX 风格特效沿用。
- 敌人/boss：billboard 精灵投影（drawEnemySprite/drawBossSprite 用 IMAGES scout/assault/heavy + 红染/光环），按 3D 距离追踪/射击；技能（如手雷轰炸）改用 3D 距离判定且可伤 boss。
- 渲染：renderWorld3D 做天花板/地面渐变、DDA 墙（ZB 遮挡 + 着色）、精灵按距离排序 + project(wx,wz) 投屏、暗角 vignette。
- 完整保留 2D 全部元系统：localStorage(mjj_tree/mjj_sel)、首页/商店/花园/HUD/胜利(直升机撤离)/死亡(血红骷髅)/卡故事/技能/选关等 UI 逻辑原样保留；标题改为「摸金行动 3D」，操作提示改为 WASD+鼠标。
- 已通过 JS 语法检查（vm.compileFunction，脚本 69896 字符；结构/关键函数/Pointer Lock/hitscan 等全部 OK）。

## 迭代：新增「宇宙血液」武器强化资源
- 需求：用户要求「做出宇宙血液等」（定位经澄清选为「武器强化资源」）。
- 实现（在 delta-loot-3d.html）：
  - 新增常量 BUFF_FRAMES=900（约15s）、COSMIC_TIERS（0~3 层：伤害×1/1.5/2.0/2.8，冷却×1/0.70/0.55/0.45）。
  - newGame 沿走廊随机刷 2-3 颗 kind:'cosmic' 发光血球（与历史/冷知识卡同属 game.loots）。
  - player 新增 cosmicStack / buffTimer 字段；applyCosmicBuff() 拾取时叠加层数并刷新计时、播粒子与 toast。
  - pickCard 分支：cosmic 不计入卡片数，只触发强化；update 中 buffTimer 递减，归零清层数。
  - playerShoot 应用 buff：有效伤害=wp.dmg*t.dmg、冷却=ceil(wp.cooldown*t.cd)；强化时枪口/曳光改宇宙紫(#b14dff/#c77dff)。
  - renderWorld3D 精灵收集区分 cosmic→drawCosmicSprite（悬浮发光血球：深紫外层+青白核心+脉动光环+「血」字）。
  - HUD 左侧面板加高，新增 🩸 强化状态行（层数/倍率/剩余秒）；锁定视角提示加一行说明。
- 已通过 JS 语法检查（vm.compileFunction，脚本 73627 字符；12 项关键接线全部 OK）。
- 注：用户原话「等」暗示可能还有更多科幻道具，代码已用 COSMIC_TIERS/lind:'cosmic' 结构便于后续扩展同类拾取物。

## 修复 + 迭代：开始键改为「单击左键」
- 重要修复：3D 版继承 2D 的「右键三连击开始」但转换时 onStart 从未被任何事件调用，导致游戏实际无法开始（仅显示提示文字）。本次一并修正。
- 需求：用户要求「将开始键设为单击左键一次」。
- 实现（delta-loot-3d.html）：
  - mousedown 重写：menu 状态左键→handleMenuClick；win/over 状态左键→onStart（直接重来/再来）；playing 状态左键→射击（锁定后）。
  - 菜单新增可点击「▶ 单击左键 开始行动」按钮（menuButtons type:'start'），handleMenuClick 识别后调用 onStart；点角色/武器仍为左键选择，互不冲突。
  - 删除无用的 rightClicks 变量与右键三连击逻辑。
  - 同步更新所有文案：菜单闪烁按钮、菜单底部提示、胜利/失败页「按 R 或 单击左键」、页脚 #help 中「开始：单击左键」。
- 已通过 JS 语法检查（vm.compileFunction，脚本 74125 字符；onStart 接线/开始按钮/右键残留 5 项全部 OK）。

## 迭代：鬼灭之刃联动（玄弥 / 黑死牟 + 武器）
- 需求：用户要求「将鬼灭之刃中的玄弥（吃鬼）、黑死牟…拥有的枪械融入进去」（用户口述有错别字，按意图实现：玄弥=噬鬼+散弹枪，黑死牟=月之呼吸刀法）。
- 实现（delta-loot-3d.html）：
  - 新增 2 个联动角色到 CHARACTERS：玄弥（紫，HP110，技能「噬鬼」）、黑死牟（银，HP160，技能「月之呼吸」）。
  - 新增 2 把联动武器到 BONUS_WEAPONS（商城金币购买）：玄弥·噬鬼散弹铳（6弹丸散射，fx='devour'）、黑死牟·月之太刀（单发高伤，fx='moon'，射程50）。
  - weaponFx 增加 devour/moon 分支；weaponRange 增加两把武器射程。
  - useSkill 新增「噬鬼」（吞噬半径7内人机+对BOSS削血，回血30并套用武器强化 buffTimer=600）与「月之呼吸」（前方 7 道扇形 hitscan，单道 dmg8，月蓝曳光）。
  - drawViewModel 枪口闪光增强：devour=恶魔尖刺爆裂（橙紫）、moon=月牙（evenodd 双弧）。
  - 菜单角色卡改为动态列宽 (464-(n-1)*8)/n 以容纳 6 张；无立绘的联动角色用矢量小人代替（drawHumanoid 思路）。
  - 加载存档后增加 selChar 越界保护。
- 已通过 JS 语法检查（vm.compileFunction，脚本 77290 字符；14 项关键接线全部 OK）。
- 排查「显示不出画面」：无头桩跑通 MENU/PLAYING 渲染（无运行时报错）；roundRect 为自定义兼容函数、ellipse 通用，旧内核兼容；结论为 WorkBuddy 内置预览面板未真正执行 file:// 的 Canvas。
- 将 delta-loot-3d.html 拷到 deploy/index.html 并用 CloudStudio 部署，生成可外部打开的在线链接：https://e84de4d30f0046e4852c56d5ccd06967.app.codebuddy.work （sandboxId e84de4d30f0046e4852c56d5ccd06967）。后续交付游戏优先给此在线链接。
- 【重大 bug 修复】游戏开始黑屏+角色不动：renderWorld3D 的精灵循环对「终点传送门」精灵（o:null）调用 project(sp.o.x ...)，解 null.x 抛 TypeError，每帧 playing 都崩，导致 loop() 中 requestAnimationFrame 永远到不了、主循环死掉→黑屏且无法移动。修复：终点精灵改用 game.exX/exZ 投影（ox/oz 带 sp.o 空值守卫）。另：loop() 整体包 try/catch，单帧渲染错误不再永久杀死游戏。验证法：把 test 钩子注入 IIFE 内部（之前插在 `})();` 之后导致 draw() 从未真正运行，是假通过），用严格 ctx 桩校验 NaN/Infinity，跑 400 帧确认无异常且玩家 z 2.20→87.56。
- 新增一批功能：①敌人大幅减速（updateEnemy sp 由 fly1.0/ground1.15 → 0.11/0.085，约玩家40~55%）+ 战术AI（dist<5.5 横向绕行+略后撤，spawn 带 strafe 字段）；②Boss 减速（updateBoss sp 0.8→0.06，dist<12 触发）；③敌人/Boss 加落地椭圆阴影增强 3D 接地感（ctx.ellipse）；④genMap 新增 5~7 座方形建筑（避开中央走廊，abs(x-cx)>1）；⑤新增 drawMinimap() 战术小地图（右上角，scaleX2/scaleZ1.35，画墙/建筑、拾取物黄紫点、敌人红点、撤离点绿三角+“撤离”字、大 BOSS 红脉冲点+“BOSS”/未现身标 ⚑BOSS区、玩家青三角朝向 rotate(angle+PI/2)），在 drawHUD 末尾调用。⑥中央保证通路加宽为 3 列（cx-1,cx,cx+1）避免碰撞半径蹭墙卡住——修前玩家 200 帧仅到 z15.82，修后 z46.84 满速。已通过语法校验（82k+）+ 200 帧无头运行时测试（走廊阻塞0、含 spawnBoss、无崩溃）。已同步 deploy 并重新部署，线上链接不变。

## 建筑物上色 + 3D 人机
- 建筑物在地图里标记为独立格值 `2`（普通墙 `1`）。`cellWall` 改为 `>= 1`。
- `wallColor(key, side, cell)` 增加 `cell===2` 分支：混凝土蓝灰（#5a6470/#79848f），与场景墙（青绿 #1b4550/#245863）明显不同。DDA 墙体循环捕获 `hitCell`，建筑列额外画楼层窗户横带。
- 小地图：建筑格（c===2）暖橙 rgba(255,176,92,.7)，普通墙冷灰。
- 敌人/Boss 改为 3D 立体矢量小人：新增 drawMech(sx,top,sw,sh,opt)（圆柱渐变躯干+肩甲+头盔目镜+手持武器+战靴+落地阴影；Boss 加犄角+威压光环）。drawEnemySprite/drawBossSprite 不再用扁平 IMAGES 贴图；地兵红、飞行兵青。
- 验证：vm.compileFunction 语法 OK（84589 字符）；严格 ctx 桩注入 IIFE 内部跑 200 帧，敌人/Boss/建筑窗户/小地图均无 NaN/Infinity 异常。已同步部署，线上链接不变。

## 角色更名
- 鬼灭联动角色显示名：玄弥→超能者1、黑死牟→超能者2（仅改 CHARACTERS 的 name 字段；技能名「噬鬼/月之呼吸」与武器名「玄弥·噬鬼散弹铳/黑死牟·月之太刀」保留不变）。HUD 通过 c.name 自动显示新名。语法 OK。

## 3D 撤离动画
- 进入 win 状态时记录 game.winFrame（本地动画时间轴）。
- drawWin 全面重写为伪 3D 透视撤离动画：黄昏天空+绿辉光、透视地面网格（放射纵线+滚动横线，向 horizon 收敛）、脉冲透视停机坪(H)。
- 直升机深度动画分 3 段：0-70 降落（z 6→2、下降）、70-150 悬停登机（小人跑向机舱、120 帧后登机消失）、150-270 拉升飞离（上升+z 推远，透视缩放 sc=1.6/z 变小）。
- 新增 draw3DHeli(cx,cy,s,t)：3/4 视角立体机身（顶面亮+侧面暗+机头玻璃反光+舷窗+尾梁透视变细+尾桨+起落架+旋转主旋翼透视盘+下洗气流锥）。
- 验证：vm.compileFunction 语法 OK（88348 字符）；严格 ctx 桩(校验 NaN/Inf)注入 IIFE 跑 300 帧覆盖降落→登机→飞离全程，无异常。已同步部署，线上链接不变。


## 子弹特效升级 + 6 角色技能 VFX 重做
- 新增特效系统：impacts(命中火花)/rings(冲击波环)/moons(月牙弹)/flash(屏幕闪光)/shake(震屏)/healOt(医疗兵持续回血)。renderWorld3D 起 shake 平移、终 restore 后 flash 叠层(lighter)。
- 子弹：hitscan 加 kind 参数；曳光按类型发光能量弹(beam/rocket/enemy/通用辉光+白芯，按距离缩放粗细)；命中火花迸溅；playerShoot 触发 flash+shake；枪口加放射火花。
- 技能 VFX：突击兵空爆弹幕(4段爆炸+环+震屏+衰减伤害)；医疗兵纳米治愈(瞬回40+持续再生+绿环)；侦察兵残影疾风(冲刺残影粒子+青环)；重装兵动能力场(护盾泡 worldR 椭圆脉动)；超能者1噬鬼领域(紫黑双环+紫闪光+吞敌强化)；超能者2月华斩(7道月牙弹飞行伤害+青环)。
- 粒子系统补全(update 更新/渲染)。
- 验证：语法 OK(99539)；严格 ctx 桩注入 IIFE 跑 320 帧(射击+6技能)，无 NaN/Inf 异常。已同步部署，线上链接不变。

## 太阳光效 + 7 场景选择系统
- 天空太阳：renderWorld3D 新增太阳绘制——核心（白黄径向渐变 22px）+ 中辉光（48px）+ 外辉光（140px 暖黄）+ 12 条旋转放射光线（每条 80-110px，阴影 shadowBlur 18，旋转动画）。太阳微浮动（sin phase）。所有场景共享太阳。
- 场景天花板/地面颜色全部提亮约 30%（各场景 cTop/cBot/fTop/fBot 改用更亮色值）。
- 场景扩展：FIELDS 从 3 个增至 7 个——城市沼泽(🏙️)、暗沼湿地(🌿)、幽暗森林(🌲)、雪域冰原(❄️)、宇宙河流(🌌)、海底遗迹(🌊)、熔岩地狱(🔥)。每个场景有独立的墙色(wallColor)、天花板/地面渐变色。
- 场景环境粒子：雪天(60 颗白色雪花飘落)、宇宙(40 星尘+2 星云光斑)、海底(35 气泡上升+8 水纹光线)、熔岩(30 火星飘升+底部热辉光)。
- 菜单新增场景选择行（7 按钮，selField 持久化于 mjj_sel 的 f 字段）。newGame 改为使用 selField 而非随机。
- Canvas 高度从 540 → 590 以容纳场景选择行 + 开始按钮。
- 验证：语法 OK(106442)；严格 ctx 桩跑 320 帧，无 NaN/Inf。已同步部署。

## 蓝色星空银河天空
- 需求：用户要求天空为蓝色，中间融合星河银河。
- 实现：
  - 天空（天花板区域）统一改为蓝色渐变（#1a3a6c → #2a5a9a → #4a88c4 → #6aacda），所有场景共享同一蓝色星空天花板。
  - 满天星点：180 颗星点分布在天空中，不同大小/颜色（暖黄/冷蓝/白），闪烁动画（sin 函数）。
  - 银河星河带：倾斜弧形带（-0.25 旋转矩形，宽 W*1.3 高56px），线性渐变从透明→半透紫白→透明，模拟银河弧。
  - 银河核心亮区：径向辉光（r=160，白→紫→透明），位于 W*0.48 / H*0.22。
  - 银河密集星簇：120 颗星沿银河带分布（偏移 sin/cos 随机），多色（粉/紫/金/蓝白），更亮更密。
  - 地面（下半屏）仍按各场景主题区分颜色；天空与地面渐变分离。
  - 太阳保留在天空中，与星空银河融合（黄色光芒叠加在蓝色天空上）。
- 验证：语法 OK(108636)；严格 ctx 桩跑 320 帧，无 NaN/Inf。已同步部署。

## 修复：无法购买月之太刀
- 问题：商城 7 把 BONUS 武器用 3 列布局，3 行总高约 640px 超出画布 590px，第 3 行（月之太刀）的购买按钮完全在画布外，鼠标永远无法点击到。
- 修复：商城布局改为 4 列（2 行），cardW/cardH/gap/marginX 缩小适配画布——4 列×2 行只需约 394px 高，购买按钮全部可见可点击。
