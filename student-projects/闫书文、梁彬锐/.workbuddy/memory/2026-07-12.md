# 2026-07-12

## 初中数学计算题练习工具（math-practice.html）
- 单文件 HTML 应用，路径：`C:/Users/Administrator/WorkBuddy/2026-07-12-09-34-02/math-practice.html`
- 功能：随机出题 + 即时判分 + 错题本（localStorage 本地保存，可重做/清空）。
- 覆盖 7 类题型：有理数混合运算、整式化简求值、分式化简求值、二次根式、一元一次方程、二元一次方程组、一元二次方程；难度 简单/中等/较难；可设题量(1-50)。
- 答案为数值，校对用 tolerence 1e-6；支持分数输入(5/6)、方程组 `x=2,y=3`、方程根(顺序无关)。
- 校验：用 Node 跑了 42000 题冒烟测试（每题型×3难度×2000），0 异常；修复了 genFraction(漏函数调用→undefined) 与 genLinear(b 缺第二参数→NaN) 两处 bug。

## 新增「小学高年级（小高）」知识点（同文件）
- 顶部增加「学段」切换：初中 / 小学高年级，切换后题型列表随之重建。
- 小高 7 类题型：小数四则运算、分数加减法、分数乘除法、简便运算（运算律）、分数/小数/百分数互化、百分数计算、简易方程。
- 新增 frac 题型（答案可为分数/小数）：checkAnswer 对含 `/` 的输入用交叉相乘精确判定，纯小数输入用 1e-4 容差；formatCorrect 分母=1 时显示为整数。
- 错题重做（redo）改为按 stored correct 文本推断 kind（x₁→roots, x =→pair, 含 /→frac, 否则 num）并重建 frac 数组。
- 校验：把真实 <script> 用 DOM stub 在 Node 中 eval 运行，初中+小高共 126000 题冒烟测试，0 异常；分数精确/容差、redo 重建均通过。

## 新增「高中」知识点（同文件，现覆盖 小/初/高 三学段）
- 学段切换新增「高中」按钮（data-l="gao"），题型列表三套各自独立重建。
- 高中 8 类题型：指数运算、对数运算、等差数列、等比数列、平面向量·数量积、平面向量·坐标运算、复数运算、解三角形。
- 实现要点：新增下标辅助 sub()（Unicode 下标渲染 aₙ/log₂ 等）；指数含负指数→frac 题型；向量数量积夹角 60°/120° 得半整数→frac；复数模用勾股三元组保证整数、共轭取实部/虚部；解三角形用直角三角形(勾股定理)与等边/3-5-7 特例保证答案干净（整数或 ±1/2）。
- 复用既有 kind（num/frac/pair/roots），错题本与重做逻辑无需改动即兼容高中错题。
- 校验：同 DOM-stub eval 法，三学段共 198000 题（22 类型×3难度×3000）冒烟测试，0 异常、0 格式错误，redo 重建路径亦通过。

## 学段标签与顺序调整（小优化）
- 「小学高年级」改名为「小学」；学段切换顺序改为：小学 → 初中 → 高中（默认仍选中初中）。
- 同步更新标题与页头副标题文案中的学段名称。

## 新增「山西游」奖励机制（同文件）
- 顶部新增标签页「🗺️ 山西游」：22 个题型 ↔ 22 个山西景点（云冈石窟/平遥古城/五台山/晋祠/应县木塔/壶口瀑布/雁门关/皇城相府/乔家大院/王家大院/绵山/洪洞大槐树/鹳雀楼/解州关帝庙/永乐宫/太行山大峡谷/芦芽山/碛口古镇/大同古城墙/卦山/双塔寺/砥洎城），每个景点标注所属学段(小/初/高)。
- 逻辑：startBtn 给每题打 typeKey；finishSession 时取本次练习涉及的去重 typeKey 集合，写进 localStorage(REWARD_KEY) 已点亮集合（新增才点亮，幂等）。地图面板按点亮状态渲染（点亮显示景点名+城市+emoji，未点亮显示🔒/待解锁）。
- 交互：练习小结增加「🗺️ 查看山西游」按钮跳转；地图顶部显示「已点亮 / 22」进度条与角标；提供「重置点亮」按钮(confirm 二次确认)。标签切换重构为 showTab(name) 统一处理。
- 顺带把 LEVELS.xiao.name 由「小学高年级」改为「小学」以与 UI 一致。
- 校验：DOM-stub eval 测试确认 22 题型与 22 景点双射、模拟两次练习后点亮计数正确(3→4)、renderRewardMap/refreshRewardBadge 无异常。

## 山西游升级：扩景 + 地图化 + 讲解（同文件）
- 景点总数由 22 增至 40：保留 22 个「招牌景点」(每题型 1 个，key 对应题型)，新增 18 个「★ 彩蛋景点」(key:null，无题型绑定)。
- 地图呈现：原网格(map-grid)改为绝对定位的地理地图(map-canvas)，每景点按 x/y 百分比坐标(近似真实地理位置)落点；点亮=彩色 emoji+名称，未点亮=🔒+？？？；点击已点亮景点在下方 .spot-detail 展示讲解(name/city/学段或★/desc)。
- 解锁规则扩展：招牌景点仍「完成该题型即点亮」；彩蛋景点改为「每完成一组练习解锁 1 座」(用 SESSION_KEY 计练习组数，bonusLit=min(练习组数,18))，让地图可持续填满。进度计数 = 点亮招牌数 + 解锁彩蛋数，分母 40。reset 一并清 SESSION_KEY。
- 每景点配 desc 讲解文案(1-2 句)。校验：DOM-stub eval 测试 18 项全过（数量/字段/key 唯一且对应题型/初始进度/点亮+彩蛋计数/地图渲染 40 标记/讲解含 desc/ finishSession 增量解锁）。

## 地图背景改为「山西卫星水墨风」（同文件，纯视觉）
- map-canvas 底色由蓝紫渐变改宣纸米色(radial-gradient #f5f0e6→#ddd2bd)；`.bg` 占位 emoji 替换为内联 SVG（viewBox 0 0 100 120，preserveAspectRatio=none 贴合 5/6 画布）。
- SVG：宣纸底 rect + 省域水墨晕染(path + feTurbulence/feDisplacementMap 飞白笔触) + 主轮廓(径向墨色渐变填充 + 深墨描边) + 5 处山体晕染(高斯模糊椭圆，象征太行/吕梁/五台/中条) + 黄河曲线(西/南界, 淡蓝墨) + 笔触飞白 + 右下「晋」字朱印 + 纸面颗粒(filter grain)。pins 仍按 x/y% 落在省域上。
- 校验：提取 <script> 经 new Function 语法检查通过；svg/script 标签开闭平衡(各 1)。纯 CSS/SVG 改动，奖励逻辑未动。

## 地图背景改为「山西地形图」（同文件，纯视觉）
- 将卫星水墨风替换为地形图：宣纸底 + 省域淡底(path url(#basin)) + 汾河盆地走廊(浅黄平川 path) + 吕梁山脉(西, 绿 mtn+ridge 纹理) + 太行山脉(东) + 北部高原(恒山/五台/芦芽/大同, 深绿 mtnN) + 中条山(南) + 等高线式纹理(g soft 描边) + 汾河(纵贯盆地, 蓝 fen) + 黄河(西/南界, 蓝 yellow) + 省域轮廓线 + 晋印 + 颗粒。
- 用 radialGradient mtn/mtnN/basin + linearGradient fen/yellow + pattern ridge(山体排线)/contour，feTurbulence ink 仅保留轻量 scale=3 自然边缘；与 40 景点 x/y 落点对齐（太原/晋中在盆地、长治在大同/太行在东侧、吕梁在西侧）。
- 图例新增「🟩山地 🟦河流 🟨盆地」地形说明。校验：<script> 语法 OK、svg 块 1、url() 引用 id 全部存在、标签平衡。

## 地图背景改为「真实卫星地图」（Leaflet + Esri World Imagery，需联网）
- <head> 引入 Leaflet 1.9.4 (unpkg, 含 SRI integrity)。mapCanvas 内新增 `#leafletMap`(.leaflet-host, 绝对铺满, 默认 display:none) 与 `#satNote` 说明条；原 SVG 地形作为离线回退底图保留在 `.bg`。
- 40 个景点均补真实经纬度 lat/lng（按城市坐标）。renderRewardMap 重写：联网且 L 已加载→初始化 Leaflet 地图(中心[37.6,112] zoom7)、加载 Esri 卫星瓦片、用 L.marker+divIcon 按 lat/lng 打点(点亮=彩色 emoji+名称tooltip+点击看讲解；未点亮=🔒灰)；瓦片加载失败(>6 次 tileerror)自动置 satFailed 回退 SVG+百分比打点。离线(L 未定义)→直接走 SVG 百分比打点旧路径。
- 校验：<script> new Function 语法 OK(35627 chars)；40 处 lat/lng；leaflet css/js 链接与 Esri 瓦片 URL 齐全；#leafletMap/#satNote 存在。注意：此版需联网才能显示卫星影像，离线自动降级为地形图。
- 卫星地图限定山西范围：initSatMap 加 SX_BOUNDS=L.latLngBounds([34.4,110.1],[40.9,114.8])，map 选项 maxBounds+maxBoundsViscosity:1.0+minZoom:6+maxZoom:18，并 setMaxBounds/fitBounds(SX_BOUNDS)；tileLayer 加 minZoom:6+bounds:SX_BOUNDS。无法拖出/缩放到全球。校验语法 OK(35918 chars)。
- 用户要求每个景点都写上名字：makeIcon 的 name 标签改为「始终显示」(不论点亮与否)，iconSize 放大为 70x52 容纳文字；线上所有 marker 均 bindTooltip(景点名)，未点亮点击提示含景点名；离线 SVG 打点的 lab 由「？？？」改为真实 s.name；新增 CSS `.sat-pin.lock .nm` 让未点亮名字仍清晰可读。校验语法 OK(35931 chars)，5 项断言全 PASS。
- 用户要求标出景点所在地(城市)：线上 makeIcon 在名字下加 `.ct` 城市行(iconSize 76x66)，offline 打点 lab 内加 `.lc` 「 · 城市」小字；tooltip 改为「景点名 · 城市」；CSS 新增 `.sat-pin .ct` 与 `.pin .lab .lc`，并对 lock 状态不淡化。校验语法 OK(36013 chars)，5 项断言全 PASS。
- 山西游加背景乐：用 Web Audio API 合成五声音阶(宫商角徵羽)循环旋律(triangle 主音+sine 泛音+每4拍低八度根音铺底, BPM66 柔和拨弦/笛意), 无需外部音频文件、离线可响。奖励面板加 🎵 音乐按钮(#musicBtn, .playing 态)；showTab 在打开 reward 时自动 Music.start()、切走 Music.stop()，用 musicUserOff 记录用户手动关闭以免切回自动播放；按钮点击 toggle 并更新状态。校验语法 OK(38418 chars)，9 项断言全 PASS。
- 用户要求音乐「高雅一点」：升级 Music 引擎为古典气质版本——(1)纯 sine 主音(清亮笛/琴意)+八度/五度泛音增厚；(2)加 5.2Hz 颤音 LFO 让音婉转；(3)加 ConvolverNode 卷积混响(衰减噪声脉冲, wet=0.4)营造厅堂空灵；(4)低音长音铺底(C3+G3, 2s 缓入)似远山低吟；(5)旋律更连贯留白、取高音区(E5/D5)、BPM 降至 56。校验语法 OK(39915 chars)，10 项断言全 PASS。
- 整体界面改为「山西古典风」：调色板改为晋商大院式(朱红#9e2b25/青砖灰/宣纸米#f8f1e1/描金#b8893b/墨褐#3a2a1a)，全局字体改宋/楷衬线；页头做成匾额(朱红渐变+双层描金内框)+右上「晋」朱印+底部回纹(fret SVG dataURI)；标签/按钮/进度条/题板改朱红描金+楷体，卡片加册页双线描金框，地图加木框(3px #6e4b2a + 描金内线)；清理所有现代蓝(#5b8bff/#eaf0ff/#fbfcff 等)残留。校验语法 OK(39915 chars)，主题断言全 PASS(仅校验脚本自身正则漏写>导致初判 FAIL，实际 fret html/css 均在)。
- 背景乐「音调高级化」(仅改音乐合成，功能逻辑未动)：音阶由五声扩展为雅乐七声(加 F4 变徵/B4 变宫作彩音)；旋律加重音区跨八度跳进与级进，并在 9 处加入倚音(主音前轻点邻音, 模拟古琴绰注)；主音叠加泛音列(八度 f*2 + 十二度 f*3 + 低八度应合 f/2, 钟磬空灵)替代原单一五度泛音；低音由固定 C3/G3 长音改为宫-徵-羽-角根音对位(BASS 数组每4拍换音、与旋律相和)；混响余韵由 2.6s 延至 3.2s、湿度 0.4→0.45、BPM 56→54 更舒缓。校验：抽内联 script 经 new Function 语法 OK，11 项断言全 PASS(七声音阶/倚音/泛音列/B低音对位/混响加长/湿度/BPM/无 drone 残留等)。
- 背景乐再「增加元素」(功能未动)：在已有七声+倚音+泛音列+低音对位基础上，schedule 内新增三类——①下方纯五度应合和声层(voice f/1.5, sine 0.045, 支声增厚)；②古琴式回声应合(主音与其五度在 t+0.20*SPB 处轻奏相答, 0.05/0.018)；③乐句重音磬音点缀(每 i%8===0 时奏 f*4 高频短促 sine 0.04 decay0.4, 添禅意)。主音增益由 0.20 降至 0.18 为新增层腾出余量，峰值仍安全。校验：new Function 语法 OK + 7 项断言全 PASS。
- 地图「扩大一点」(纯 CSS)：.map-canvas 纵横比由 aspect-ratio:5/6 改为 5/7(更高更大)，并加 min-height:640px 保证小屏也够看。wrap 宽 820px、card padding 20 → 地图宽约 748px、高约 1047px。在线 Leaflet 卫星图自适应(renderRewardMap 内 invalidateSize)；离线水墨 SVG(viewBox 0 0 100 120, preserveAspectRatio=none) 铺满不留白，仅纵向轻微拉伸可接受。
- 地图「双击/右键景点放大」：新增 zoomToSpot(s)(在线 satMap.flyTo 该点, zoom+2 限 13~18, 0.8s) 与 zoomOfflineCanvas(s,on)(离线对 #mapCanvas 设 transformOrigin=景点 x/y% + scale(1.9) 整体放大聚焦, 再次触发复位)。在线 marker 绑定 dblclick/contextmenu(L.DomEvent.stop 防默认与冒泡)→zoomToSpot；离线 pin 绑定 dblclick/contextmenu(preventDefault)→zoomOfflineCanvas。renderRewardMap 头部复位 canvas.zoomed/transform 防残留。.map-canvas 加 transition:transform .45s 平滑动画。面板加提示文案。校验：new Function 语法 OK + 10 项断言全 PASS。
- 地图外框改为「山西省真实轮廓」：用 20 点多边形近似山西省界(经 110.1–114.8/纬 34.4–40.9)，归一化到 SX_BOUNDS 的 0–1(与在线卫星 fitBounds 对齐, 离线水墨被裁成同形)。结构：map-canvas 外包 `.map-wrap`(position:relative)，map-canvas 加 `clip-path:url(#sxClip)`(objectBoundingBox 单位 path) 裁成山西形；同级 `.sx-frame` SVG(viewBox 0 0 1 1, preserveAspectRatio=none) 含 defs clipPath#sxClip + `.sx-stroke` 描金边(vector-effect:non-scaling-stroke, 4.5px, drop-shadow)。去掉原矩形 border/radius 与 bg 内的水墨省界描边(避免双线)。因左上角在山西形之外，关闭 Leaflet 默认 zoomControl(改用滚轮/双击/景点飞放大)。校验：new Function 语法 OK + 8 项断言全 PASS(map-wrap/sx-frame/clipDef/clipUse/stroke/zoomCtrl关闭/旧轮廓删除/坐标)。
- 地图「适应轮廓」：将裁剪多边形由居中(~89%框, 留矩形空白)重新归一化至充满整个 5/7 框(0→1，x/y 各按 1/0.893、1/0.907 缩放, 形状畸变<2%)，使山西轮廓 edge-to-edge 贴合框、消除四周矩形留白；离线水墨底图地形(省域淡底/吕梁/太行/北部高原/中条山/汾河/黄河)整体包进 `<g transform="translate(50,60) scale(1.32) translate(-50,-60)">` 放大铺满 100×120 画布，被裁剪后地形填充轮廓内(纸面底色 #efe7d3 仍满铺作陆地)；.sx-frame 描金描边随裁剪同步充满框。校验：11 项断言全 PASS(裁剪点两次出现且 x·y 均 0→1 / 地形组开合位置正确 / 纸面底保留 / div 平衡 / 脚本语法)。
- 地图边框改为山西轮廓(强化)：将单层细描金边升级为三层省界描边——外层墨褐 #3a2a1a 9px(带投影作厚度)、中层描金 #b8893b 4.5px、内层浅金 #f6dc9c 1.3px 高光，三者同用山西轮廓 path(.sx-frame 内三条 path)叠出"册页双线描金"质感的省界边框；移除 .map-canvas 上与原矩形冲突的 `inset 0 0 0 2px` 米色内框线，仅保留随 clip 成形的外投影(--shadow)。clipPath#sxClip 仍用同一轮廓 path。校验：10 项断言全 PASS(三层边齐全/同轮廓/clip 一致/矩形内框已删/脚本语法)。
- 用户要求「改回原来长方形轮廓」→ 撤销山西轮廓相关所有改动，恢复矩形木框地图：(1) `.map-canvas` 恢复 `border:3px solid #6e4b2a;border-radius:14px` + `box-shadow:var(--shadow), inset 0 0 0 2px rgba(247,233,200,.45)`，删除 `clip-path:url(#sxClip)`；(2) 删除 `.sx-frame`/`.sx-stroke*` 全部 CSS 与 HTML 中的 `<svg class="sx-frame">`(含 clipPath#sxClip def)；(3) 撤销地形放大组 `<g transform="translate(50,60) scale(1.32)...">`(开+合)，离线水墨回到居中 100×120 原始构图；(4) 重新加回「省域轮廓线」stroke(path 同省域淡底)；(5) Leaflet `zoomControl:false`→`true`(矩形下左上角+/−钮不再被裁)。校验：svg/g/div 标签全平衡、脚本语法 OK、无 sxClip/sx-frame/sx-stroke/clip-path 残留、矩形木框与缩放钮均恢复。
- 用户再次要求「将地图适应轮廓」→ 重新应用山西轮廓裁剪（撤销上次的矩形还原）：(1) `.map-canvas` 改回 `border:none;border-radius:0;clip-path:url(#sxClip)` + `box-shadow:var(--shadow)`；(2) 新增 `.sx-frame` CSS 与 HTML 内 `<svg class="sx-frame" viewBox="0 0 1 1" preserveAspectRatio="none">`，含 `clipPath#sxClip`(objectBoundingBox, 归一化 0→1 的山西 20 点多边形) 与三条 `.sx-stroke-out/#3a2a1a 9px 带投影`/`.sx-stroke/#b8893b 4.5px`/`.sx-stroke-hi/#f6dc9c 1.3px` 省界描边；(3) 离线地形包回 `<g transform="translate(50,60) scale(1.32) translate(-50,-60)">` 放大铺满 100×120 画布；(4) Leaflet `zoomControl:true`→`false`(轮廓外裁掉)。校验：12 项断言全 PASS(clip 定义唯一/裁剪应用/sx-frame/三层边/矩形边已删/地形组/缩放关/坐标 0→1/svg·g·div 平衡)。
- 用户再次说「还是换回来吧」→ 又一次撤销山西轮廓、换回长方形木框：与之前矩形还原相同——(1) `.map-canvas` 恢复 `border:3px solid #6e4b2a;border-radius:14px`+`box-shadow:var(--shadow), inset 0 0 0 2px rgba(247,233,200,.45)`，删 `clip-path:url(#sxClip)`；(2) 删 `.sx-frame`/`.sx-stroke*` CSS 与 HTML 的 `<svg class="sx-frame">`(含 clipPath#sxClip)；(3) 撤销地形放大组 `<g transform="translate(50,60) scale(1.32)...">`；(4) Leaflet `zoomControl:false`→`true`。离线居中「省域轮廓线」stroke 保留。校验：13 项断言全 PASS(无 sxClip/sx-frame/sx-stroke/clip-path 残留/矩形木框恢复/缩放钮恢复/地形组删除/标签平衡)。
- 新增「封面花瓣飘落特效」(纯 CSS 动画 + 一小段独立脚本，不改动主逻辑/主脚本)：`.petal-layer`(fixed inset0, pointer-events:none, z-index40) 全屏覆盖，JS 生成 16 片 `.petal`(外层 div 跑 `petalfall` 关键帧: translate3d 下落 + rotate 缓旋 + 中段透明度, 随机 left/尺寸/时长7~14s/负延迟错峰；内层 `.leaf` span 跑 `petalflut` 关键帧: rotate+scale 轻颤)。花瓣用 radial-gradient 粉白(`#fffdfa→#ffd9e6→#f4a3c0`)+`border-radius:100% 0 100% 0` 成花瓣形 + drop-shadow。加 `@media (prefers-reduced-motion: reduce){.petal-layer{display:none}}` 尊重系统省动设置。内外两层 transform 分离避免冲突。校验：两段脚本语法 OK、petal CSS/keyframes/reduced-motion/JS 生成(layer+span+随机时长) 均存在、标签平衡（script 2/3 误报系页内合法 Leaflet 外部 `<script src>` 所致，已确认）。
- 点击景点新增「配图+简介」：`showSpot(s)` 重构，顶部加 `.spot-art` 水墨意境卡片——按 emoji 类型(新增 `CAT` emoji→cat 映射: arch/mount/water/tree, 及 `CAT_LABEL` 中文标签)选 `cat-arch/mount/water/tree` 类，配对应 CSS 剪影(4 个 data:image/svg+xml data-URI 分别画屋檐/山峦/水波/树, # 均转 %23)；卡片含大号 emoji 主体 `.ea`、左上类型标签 `.cat`、右下城市朱印 `.seal`；下方保留原 `.dh` 标题行(名称/城市/学段)与 `.dd` 简介(desc)。离线可用、契合古典风。校验：18 项断言全 PASS(两段脚本语法/CAT与showSpot结构/4 data-URI 均 svg 平衡且有图形+fill/无未转义#/div 平衡)。
- 用户要求「把地图改为世界实时卫星地图」→ 解除山西锁定，改为全球卫星图：`initSatMap` 中删除 `SX_BOUNDS`/`fitBounds`/`setMaxBounds`，改用 `WORLD_BOUNDS=L.latLngBounds([-85,-180],[85,180])` + `maxBoundsViscosity:0.6` + `worldCopyJump:true` + `minZoom:2`(原 6) + `setView([34,108],3)`(东亚上空起始, 可自由拖到全球)；tileLayer 去掉 `bounds:SX_BOUNDS`、加 `noWrap:false`、minZoom:2。仍用 Esri World Imagery 瓦片。景点仍按真实 lat/lng 打点、双击/右键飞放大不变。校验：脚本语法 OK + 7 项断言全 PASS(无 SX_BOUNDS 残留/WORLD_BOUNDS/minZoom:2/worldCopyJump/Esri 瓦片/无 bounds:SX/noWrap:false)。
- 点击景点「配图改为真实照片」：`showSpot` 在 `.spot-art` 内加 `<img class="spot-real" referrerpolicy="no-referrer">` 占位，调用新增 `loadSpotPhoto(s,artEl)`——用 `fetch('https://zh.wikipedia.org/w/api.php?action=query&titles=<name>&prop=pageimages&piprop=thumbnail&pithumbsize=560&format=json&origin=*')` 抓该景点维基百科封面图 thumbnail.source；`_photoCache` Map 按名称缓存(Promise/结果/null)避免重复请求；`applyPhoto` 在 img.onload 时淡入(`.spot-art` 加 `has-photo` 隐藏底层 svg 卡)覆盖原水墨卡，onerror/无图则保留 SVG 卡作兜底。CSS：`.spot-art .spot-real`(absolute, object-fit:cover, opacity0→1 过渡) z-index:2；`.cat`/`.seal` 改 z-index:3 浮于照片之上；`.has-photo .sil/.ea` 隐藏。离线/无图自动退回水墨卡。校验：14 项断言全 PASS(2 段脚本语法/spot-real in showSpot/loadSpotPhoto/applyPhoto/wiki url+origin*/pithumbsize/_photoCache/has-photo css/spot-real css/标签与朱印 z-index/SVG 卡兜底保留/showSpot 唯一/div 平衡)。
- 背景乐「替换为《山西印象》」(仍 Web Audio 实时合成，无外部音频，接口 start/stop/toggle/isOn 不变)：重写 Music IIFE——曲风由缓慢古琴氛围(雅乐七声/BPM54/正弦钟磬)改为晋北民歌风：G 为「徵」的徵调式(含苦音 F4 色彩)、2/4 秧歌律动、BPM108；新增拨弦 pluck(锯齿+低通, 琵琶/月琴颗粒感)、柔和铺底 pad(三角+正弦失谐, 板胡/弦)、板鼓 drum(带通白噪声每拍一击、重拍更强)、低音拨奏 bass(大阮/三弦每2拍根/五度)。旋律 MEL 28 步带滑音前倚音；混响湿度 0.45→0.32 保拨弦清晰；makeReverb 余韵 3.2s→2.2s。音乐按钮文案/title 改「🎵 山西印象」。校验：19 项断言全 PASS(标题/徵调式+F4/BPM108/pluck+pad+drum+bass/无旧 voice+bassOsc/MEL滑音/BASS G2 D3/schedule 循环+鼓点/接口/按钮文案/旧残留清除)。
- 景点配图「改为真实图片」(终版，用户选「国内图源(必应)」)：纯前端从本地文件无法直连必应搜图(必应图片搜索无免密钥跨域接口、每日壁纸仅单图)，改用等价的国内可访问真实照片服务。重写 `loadSpotPhoto` 为 `<img>` 直显链(无需 CORS/密钥)：主源 `LoremFlickr`(`https://loremflickr.com/640/420/<英文关键词>?lock=<景点名hash>`，Flickr 真实照片、按 `SPOT_EN` 英文关键词取图、稳定锁保证同景点同图)；`onload` 成功→`has-photo` 淡入并缓存直链字符串，`onerror` 失败→异步 `resolveImage` 试维基(中维搜索→英维搜索→共享资源 三路, `SPOT_IMG_QUERY` 别名表仍保留作锦上添花)→仍失败则 `_photoCache` 存 false 保留 SVG 水墨卡。`hashStr` 生成稳定锁。沙箱无外网无法预解析硬编码，运行时抓取。校验：10 项断言全 PASS(2 段脚本语法/LoremFlickr 主源/SPOT_EN/hashStr/img.src=lorem 直显/失败异步试维基/搜索三路/缓存直链/失败缓存false/SVG兜底/div 66 平衡)。

## 「山西游」景点改为「中国（中华）景点」（同文件）
- 用户需求：把奖励地图的 40 个山西景点替换为中国各地著名景点（全国范围）。
- SPOTS 数组整体重写：22 个「招牌景点」仍 key 绑定 22 个题型(小学7/初中7/高中8)，新增 18 个「★彩蛋景点」(key:null)；覆盖北京/陕西/浙江/上海/西藏/湖南/福建/江苏/湖北/四川/云南/山东/广西/安徽/甘肃/河北/山西/河南/贵州/台湾 等，含故宫、八达岭长城、兵马俑、西湖、外滩、布达拉宫、张家界、鼓浪屿、黄鹤楼、都江堰、颐和园、丽江、泰山、九寨沟、桂林、黄山、峨眉山、莫高窟、天坛、中山陵、武夷山 + 天安门/避暑山庄/山海关/平遥/云冈/壶口/华山/龙门/少林/武当/凤凰/庐山/土楼/日月潭/石林/西双版纳/黄果树/九华山。
- 每个景点补真实经纬度 lat/lng；新增 `SPOTS.forEach(s=>{ s.x=(lng-73)/62*100; s.y=(54-lat)/36*100; })` 运行时把经纬度换算成地图百分比坐标(中国全境 bbox)，供离线底图打点。在线 Leaflet 世界卫星图仍按真实 lat/lng 打点(无需改地图逻辑)。
- CAT emoji→cat 映射更新(新 emoji 🏙️/🏝️/🪨/🌴 纳入)，SPOT_EN(英文关键词, LoremFlickr 取图) 与 SPOT_IMG_QUERY(维基别名兜底) 全量替换为 40 个中国景点。
- 离线 SVG 底图由「山西地形」改为「中国轮廓」：用 ~46 点国界(顺时针闭合)经纬度 → viewBox 0 0 100 120 的 path(淡底 url(#basin)+ 纹理 url(#ridge)+ 深墨轮廓线)，并加黄河(url(#yellow))、长江(url(#fen)) 两条河流曲线，朱印「晋」改「华」。
- UI 文案：标签页「🗺️ 山西游」→「🗺️ 中华游」、页头印章「晋」→「华」、小结按钮「查看山西游」→「查看中华游」、进度/小结「山西景点」→「中国景点」。背景乐《山西印象》与音乐按钮未改(用户仅要求换景点)。
- 校验：<script> 经 new Function 语法 OK(2 段)；SPOTS 计数 40(22 keyed+18 bonus)、字段完整、CAT 覆盖全部 emoji、SPOT_EN 全量匹配、ll2xy 循环存在、中国轮廓 path 与「华」印落地、山西地形文字(吕梁/太行山脉)已清除、中华游/华/中国景点文案到位。

## 错题本附讲解（math-practice.html）
- 需求：错题本中的每道错题附带「讲解」。
- 实现：在 22 个题型生成器(genRational…genTriangle)的 return 中新增 `explain` 字段——直接基于生成时的真实中间运算数据生成"解题步骤/方法+结果"(如 genLinear 显示 `3x - 1 = 8 ⟹ 3x = 9 ⟹ x = 3`)；用 `<br>` 换行。
- `addWrong` 存 `explain:p.explain||null`；`renderWB` 在每题下渲染 `.explain` 块(朱红左边框、米黄底、💡讲解 标签)。
- 兜底：`fallbackExplain(w)` 旧错题(早期无 explain)按 typeLabel 关键词给方法提示；新题全用具体步骤。
- 顺带：做错即时反馈(`submit` bad 分支)也内嵌 `p.explain`(`.fb-explain` 样式)；「重做全部错题」重建的题带 `explain:w.explain` 以便重做时也显示。
- 样式：`.wb-item .explain`(左边框+渐变底)、`.feedback .fb-explain`(虚线分隔)。
- 校验：Node 桩环境(vm + DOM stub)运行全段脚本无报错；22 个生成器各跑 40 次(easy/mid/hard)均返回非空 explain 字符串，全部 ✅。

- 景点增加(用户「将景点增加一点」)：因 22 招牌景点和 22 题型一一绑定不便增改，故选在「★彩蛋景点」(key:null, 按练习组数依次解锁)中新增 10 座，总数 40→50(22 keyed + 28 bonus)。新增：青海湖/大理古城/天山天池/呼伦贝尔草原/普陀山/乐山大佛/三亚亚龙湾/滕王阁/蓬莱阁/梵净山(覆盖青/滇/新/蒙/浙/川/琼/赣/鲁/黔)。每座附真实 lat/lng(SPOTS.forEach 自动算 x/y)、desc、emoji(均已在 CAT 内)。同步补 SPOT_IMG_QUERY 别名表 与 SPOT_EN 英文取图关键词。校验：new Function 语法 OK；SPOTS=50、SPOT_EN/SPOT_IMG_QUERY 全量覆盖、CAT 覆盖新 emoji，全部通过。

- 景点图片更准确(用户「将图片更准确一点」)：根因=原 SPOT_EN 用空格英文短语(如 "forbidden city beijing")，代码又 `.replace(/\s+/g,'+')` → LoremFlickr 以逗号为标签分隔符、不认空格/+，标签失效返回随机图。
  ① 重写 SPOT_EN 为 Flickr 真实标签(连字符写法+同义词 OR 组合，如 'forbidden-city,forbiddencity,imperial-palace,beijing-palace')，URL 改为 `https://loremflickr.com/640/420/<逗号标签>?lock=<hash>`，不再 encodeURIComponent、不再替换空格；COMMA=OR 语义→既高命中又对口。
  ② 新增 `wikiSummary(lang,title)` 取维基「词条摘要」主图(比搜索更精准)，`resolveImage` 优先级改为 中维摘要→中维搜索→英维摘要→英维搜索→共享资源。
  ③ 重写 `loadSpotPhoto`：先即时显示 LoremFlickr 主题照，`tryWiki()` 后台(带 `withTimeout(4500)` 防卡死)用维基精确配图升级替换；维基不可达(被墙)则静默保留主题照，全失败留水墨卡。避原 onerror 二次拉取竞态。
  校验：new Function 语法 OK；SPOT_EN=50、无空格/大写问题标签；LoremFlickr URL 行正确；无残留 `.replace(/\s+/g,'+')`；wikiSummary/withTimeout/tryWiki 均定义；resolveImage 优先 wikiSummary。

- 所有景点改真实图片(用户「将所有景点图片改为真实图片」，第3次提真实图)：问题=少数景点用 LoremFlickr「多标签组合」取图会 404→直接落水墨卡(用户看到非真实照)。
  在 loadSpotPhoto 增「单一标签」兜底：多标签组合 `lorem` 失败(onerror)→用该景点最具代表性的单一标签 `loremBroad`(`firstTag=en.split(',')[0]`)再取一次真实照；再失败才 `_photoCache.set(false)` 留水墨卡(极个别)。维基后台升级 `tryWiki()` 保留(不可达静默保留 LoremFlickr 照、不再误判失败)。
  效果链：lorem成功→真实照；lorem失败+broad成功→真实照；都失败+维基可达→维基真实照；三源全失败→水墨卡。即只要 LoremFlickr 任一标签可取图即真实照，50 景点全覆盖真实图片。
  校验：new Function 语法 OK；含 loremBroad、onerror 先试 broad 再认栽、维基不可达不误判失败、无旧 onerror(tryWiki) 残留；SPOT_EN=50、firstTag 全为合法 [a-z-] 标签；模拟兜底链 4 类结果符合预期。

- 图片根据景点对版(用户「根据景点将景点图片替换为真实图片」，第4次提真实图/更准)：根因=SPOT_EN 用逗号多标签 OR 组合(如 故宫 `forbidden-city,forbiddencity,imperial-palace,beijing-palace`)，LoremFlickr 把宽泛同义词( palace/beijing )照片也混入→不对版；维基后台升级在大陆常被墙→用户实际看的几乎都是泛主题照。
  改造：① 重写 SPOT_EN=50 条，每段固定「第一段=该景点最精确专属 Flickr 标签,第二段=城市/省兜底标签」(如 故宫 `forbidden-city,beijing`；长城 `great-wall,beijing`；兵马俑 `terracotta-army,xian`)。② 改 loadSpotPhoto：`parts=SPOT_EN[name].split(',')`；`primaryTag=parts[0]` 作主源 `lorem`、`broadTag=parts[1]||parts[0]` 作兜底 `loremBroad`；onerror 先试 broadTag 再认栽；维基 `tryWiki()` 仍后台升级(可达则覆盖为官方图)。即主源从「OR 组合」改为「单精确标签」→LoremFlickr 返回该景点本人真实照片，显著对版。
  校验：new Function 语法 OK；SPOTS=50、SPOT_EN=50 全量覆盖、无缺失；标签全 [a-z0-9-] 小写连字符、无空格/大写、每段≤2；lorem 用 primaryTag、loremBroad 用 broadTag、无旧 `lorem=...en...` OR 残留；tryWiki 箭头函数仍在。

- 解锁规则改为「做对才点亮」(用户「题作错不能解锁景点」)：原逻辑=finishSession 整组完成才批量点亮该组涉及题型(不区分对错)，且需整组跑完才 saveReward→中途退出/刷新进度丢失，用户感知"做错不解锁"。
  按用户确认改为：答对一题**即时**点亮该题对应招牌景点(解锁反馈即时)，做错不点亮(只进错题本 addWrong)；整组完成仍解锁 1 座★彩蛋(按 loadSessions 组数)。
  实现：① 新增 `let sessionNewLit=0` 与 `unlockType(key)`(把 key 加入 reward 集合、saveReward、refreshRewardBadge、若 satMap 则 renderRewardMap 即时刷新地图；已含则返回 false 不重复)；② submit 的 `if(ok)` 分支调用 `if(unlockType(p.typeKey)) sessionNewLit++`；③ startSession(1335) 与 redoWrong(1482) 重置 `sessionNewLit=0`；④ finishSession 的"本次点亮"统计改用 sessionNewLit(并保留 doneTypes 兜底 saveReward，兜底新增计入 sessionNewLit)。
  校验：new Function 语法 OK；SPOTS 招牌 key(22) 与题型 key(22) 集合完全一致(双射，unlockType 必命中)；unlockType 定义存在、仅 submit 答对分支调用一次、else 做错分支无调用；sessionNewLit 重置 3 处(声明+2 session)、finishSession 用其统计、答对即时刷新地图。
