# 2026-07-12

## 晋楹记 · 匠木风格视觉重做（延续 111.docx 需求）

- 用户发 `@"C:/Users/Lenovo/Desktop/111.docx" 按这样做`：把晋楹记视觉重做成手游《匠木·第五大发明》风格（朱砂红底、玉兰装饰、传统描边、宣纸卡、书法标题、3D 浮木木块）。
- docx 内无文字，仅 3 张嵌入 jpeg（匠木启动/玩法/科普卡截图），已用 Python zipfile 提取，session 末清理（原 docx 在 Desktop 未动）。
- 重做内容：
  - `style.css` 整体换肤（`:root` 朱砂 `--zhu:#9b2c24`、宣纸 `--paper:#f0dbb8`、木色 `--wood`、金 `--gold`、玉 `--jade`；body 玉兰径向装饰 + `.rock-decor` 假山 + `.frame-wrap` 描边 + `.seal` 红印 + `.science-card` + `.anim-stage-container` 分层搭建动画；引入 Ma Shan Zheng/Noto Serif SC 字体）。
  - `index.html` 加 `.rock-decor`、`<main id="view">` 包 `.frame-wrap`；弹窗 id 改为 `mask/mTitle/mBody/mActs`。
  - `app.js` 榫卯积木改为 3D 立体 SVG（`pcs*3D` 系列：阴影层偏移 + 木纹 linearGradient + 高光 + 刻线，viewBox 0 0 52 38）；`partSVG` 据此生成。
- 修复两处重写引入的 bug：
  - 非法 Unicode 转义 `\ud83e\uddxa9`（🧩 应为 `\ud83e\udde9`）→ 已修正，`node --check` 通过；全量扫描 2350 个 `\u` 转义无非法/代理对错误。
  - `layerRole()` 正则/返回值被写成 `\\u`（双反斜杠）导致永远匹配失败、返回字面 `\u...` 字符串 → 改为直接用中文字符，已验证 横梁/立柱/瓦面/未知 返回正确。
- 验证：用 Node `vm` + DOM shim 写了 smoke 测试（临时 `smoke.js`，已删），覆盖渲染/3D SVG/闯关答题/古建自动拼装完成（含应县木塔抗震特殊分支）/合成 SVG/藏品馆/社区/我的/自由拼装/结构倾斜预警——31/31 通过。
- 临时文件 `111_img*.jpeg`、`docx_media/`、`smoke.js` 已清理，最终仅 `index.html / style.css / app.js / data.js`。

## 古建区升级为 Three.js 3D 斗拱拖拽吸附游戏（新需求）

- 用户追加需求：古建区改成单 HTML 文件、CDN ES Module 加载 Three.js(r160)、数据驱动关卡。
- 新增 `gudai-3d.html`（独立单文件，与 2D 站点并列）：
  - 五个模块（中文注释）：①场景初始化 ②物体创建(柱+卯眼/斗拱+榫头) ③交互控制(Raycaster 拖拽到水平面 + 滚轮旋转 Y) ④碰撞检测(平面距离<0.15 且 Y 轴角偏差<10°→吸附) ⑤反馈动画(吸附青绿高亮+Web Audio 咔嗒音+失败红闪弹回+镜头环绕一周+下一关按钮)。
  - `levels` 数组驱动：每关含 name/building/description/targetPosition{x,y,z}/targetRotation{x,y,z}/tolerance(默认0.15)/nextButtonText；`loadLevel(i)` 按数据重建柱与斗拱并定位相机/地面。
  - 配色：朱红 #9B1A1A 主、青绿 #2E8B57 辅、木纹黄褐 #D2B48C 底；衬线 Noto Serif SC；左上关卡名、右上"拖拽斗拱，对准柱头卯眼"。
  - 几何对齐已数值验证：榫头底面 y=1.100 与柱头卯眼顶面 y=1.100 完全重合，柱底精确落在地面。
  - 语法 `node --check`（抽 module 脚本）通过；快照逻辑用例通过。
- 与 2D 站点关系：当前为独立可双击打开的 3D 单文件，未改动 index.html 古建区导航（用户未要求合并）。

## 古建区并入主站（只改古建区，其余不变）

- 用户要求：只把古建区换成新的 3D 斗拱拼装，闯关区/藏品馆/社区/我的完全不变。
- 实现：仅改 `app.js` 的 `renderBuild(main)` 一个函数——提前 `return` 并 `main.innerHTML` 内嵌 `<iframe src="gudai-3d.html" class="gudai3d-frame">`；旧的图纸复刻/自由拼装逻辑保留为不可达代码（未删，便于回退）。
- `style.css` 新增 `.gudai3d-frame{width:100%;height:78vh;min-height:560px;border:0;background:#D2B48C}`。
- 其余四个 render 函数（renderChallenge/renderMuseum/renderCommunity/renderProfile）一行未动。`node --check` app.js+data.js 均通过。
- 注意：iframe 从 https CDN 以 ES module 方式加载 Three.js，file:// 双击打开时 Chrome/Edge 可正常加载（import 的是 https 绝对 URL）。

## 修复：古建区空白（Three.js 离线化）

- 现象：点「古建区」3D 页空白。根因：原 `gudai-3d.html` 用 `importmap` + `<script type="module">` + `https://unpkg.com/three@0.160.0/build/three.module.js`，预览/浏览器连不上 CDN 时整个模块不执行 → 空白。
- 修复：改为**本地离线 + 经典脚本**。已把 UMD 全局版 Three.js（r128，603KB）下载到工作区 `three.min.js`；`gudai-3d.html` 去掉 importmap/module/import，改用 `<script src="three.min.js"></script>`（classic），游戏脚本也改为 classic `<script>`；色彩空间写兼容判断（`THREE.SRGBColorSpace` 有则用 `outputColorSpace`，否则 `outputEncoding`）。新增 THREE 未定义兜底提示，避免再次"静默空白"。
- 验证：抽内联脚本 `node --check` 通过；无 r160 专属调用残留；`three.min.js` 头部 `!function(t,e){...}` 确认是 UMD 全局版（挂 `window.THREE`）。
- 结论：`file://` 双击与本地预览均不再依赖网络。three.min.js 必须与 gudai-3d.html 同目录。

## 全站五模块升级为沉浸式 3D（用户选「沉浸式3D场景」）

- 用户「你能所有模块都做成3D吗」→ AskUserQuestion 选「沉浸式3D场景」。
- 新增 4 个独立 Three.js 场景（均本地离线引入 `three.min.js` + 复用 `data.js` + 共享 localStorage `jinyingji_save_v1`；classic script，跨 script 共享 const LEVELS/PARTS/BUILDINGS）：
  - `chuang-3d.html` 闯关区：木作工坊场景(旋转斗拱背景+暖光+木地板+收纳架)，HTML答题面板悬浮；答对写 inventory 并 3D 掉件抛物飞入收纳架；关卡选择/答题/解析/通关。
  - `cang-3d.html` 藏品馆：3D展厅一排展台，BUILDINGS 按类型生成简化模型(木塔5层/大院2x2/双塔/悬空错落/单檐殿)，已收录自转+浮动、未收录灰影；轨道相机(拖拽环绕+滚轮缩放)，点展台弹信息卡；读 state.built。
  - `community-3d.html` 社区：环形回廊悬浮画屏(示例作品+user.freeWorks)，自动旋转+拖拽+左右翻页+点赞，正对镜头者放大。
  - `profile-3d.html` 我的：数据殿堂，战绩(零件/古建/关卡/木屑)化为高低石柱(生长动画)，中央功德碑，右上HTML报告卡+导出txt；等级 学徒/匠人/匠师/大匠师。
- 主站接入：`app.js` 的 `render()` 改为 `VIEW_3D` 映射表，五视图统一 `iframe src=xxx-3d.html?t=Date.now()`（cache-bust 保证读最新存档）；旧 2D render 函数保留为不可达回退。
- 验证：`node --check` app.js/data.js + 抽取四个3D页内联脚本均通过。
- 当前工作区文件：index.html / app.js / data.js / style.css / three.min.js / gudai-3d.html / chuang-3d.html / cang-3d.html / community-3d.html / profile-3d.html。

## 合并为单一网页「晋楹记」（用户最终要求）

- 用户「把这结合成像原来一样的一个网页，要求能直接在浏览器中打开」：把五个分散的 `*-3d.html` 合并回**一个单页应用**（像最初 index.html 那样的整页体验），顶部标签在本页内切换，不再 iframe 跳独立页。
- 实现：
  - 新增 `style.css`（顶部 `#topnav` + 五个场景覆盖层样式去重合并：`.hud/#loading/#resbar/#panel/.lv-* /.opt/.explain/.btn/#reward/#card/#tip/#bar/.cbtn/#report/#desc/#hint/.flash/#next-btn/#reset-btn`）。
  - 重写 `app.js`（IIFE 单页控制器）：共享 `state`（从 localStorage `jinyingji_save_v1` 统一加载，五个场景只读同一份并 `saveState()` 互通）；把五个场景改成「接收 `container` 的工厂函数 `sceneChallenge/sceneBuild/sceneMuseum/sceneCommunity/sceneProfile`，返回 `{dispose}`」；`mount(name)` 切换时先 `currentDispose.dispose()`（取消 RAF、移除 window 监听、renderer.dispose、移除 canvas），再 `view.innerHTML=''` 并挂载新场景；控制器顶部 `#topnav` 按钮 bind click。
  - `index.html`：`<nav id="topnav">`(品牌+5按钮) + `<div id="view">` + 顺序加载 `three.min.js`/`data.js`/`app.js`。
  - 每个场景的画布尺寸改用 `container.clientWidth/Height`（适配导航下方区域）；`document.body` 挂画布改为 `container.appendChild`；`window` 级 resize/pointer 监听统一经 `addWin` 登记并在 `dispose` 时移除；内联 `onclick` 依赖的全局函数（startLevel/answer/nextQ/renderLevels/rotateTo/likeCur/exportReport）在 dispose 时 `delete`。
- 删除：`chuang-3d.html/cang-3d.html/community-3d.html/profile-3d.html/gudai-3d.html`（已合并进 app.js）。
- 验证：`node --check` app.js/data.js 通过；写宽松桩冒烟测试 `_smoke3d.js`（`mk()` THREE Proxy + 假 DOM，`vm` 加载 data.js+app.js，触发 DOMContentLoaded，按序 fire 五个导航 click 各两次含来回切换）捕捉到真实 bug：`mount` 误把场景工厂返回的 `{dispose}` 对象当函数调用 → 改为 `currentDispose.dispose()` 修复；重跑 8 次 mount/dispose 循环全部 OK，无报错。测试文件已删。
- 最终工作区（5 文件，双击 `index.html` 即可运行，无需联网）：index.html / app.js / data.js / style.css / three.min.js。

## 古建区新增三关（用户：只剩古建区加，其它不变！！！）

- 需求：古建区在原有「晋祠圣母殿·柱头斗拱」基础上**增加三关**，其余四模块（闯关/藏品/社区/我的）一行不改。
- 四关 `levels` 数据驱动（仅改 `app.js` 的 `sceneBuild` 函数，222–535 行整段重写，其它场景工厂 `sceneChallenge/sceneMuseum/sceneCommunity/sceneProfile` 未碰）：
  1. 晋祠圣母殿·柱头斗拱（斗拱，默认木色，next「前往佛光寺」）
  2. 佛光寺东大殿·梁枋搭接（燕尾榫梁枋，深褐柱 #5C3317，target{x:2,y:1.5,z:0,rz:0}，tol 0.15，next「前往应县木塔」）
  3. 应县木塔·斗拱层层叠（朱红柱 #8B1A1A，sequential 三构件依次解锁：栌斗→华拱→令拱，targets {0,1.2,0}/{0,2.0,0.8}/{0,2.8,0}，tol 0.2，next「前往悬空寺」）
  4. 浑源悬空寺·飞梁插崖（岩壁岩灰 #696969、不规则长方体代替圆柱，长直榫横梁，target{x:-1.5,y:1.0,z:0,rz:15°倾斜}，tol 0.18，next「恭喜通关」）
- 关键改造（保持接口 `return {dispose()}` 不变）：
  - `pieces[]` 支持多可拖拽构件 + `activeIndex` 顺序解锁（首块可见可拖，拼好后 `visible=true` 解锁下一块，HUD 提示「第N块已就位…」）。
  - `createColumn(L)` 按 `L.rock` 分支：圆柱(默认) / 不规则长方体岩壁；颜色取 `L.columnColor`。
  - `createPiece(type)` 三种榫头：斗拱(box 组) / 燕尾榫(ExtrudeGeometry 梯形上宽下窄) / 长直榫(细长方块)；榫头底面统一在本地 y=-0.45 与柱顶卯眼对齐。
  - 吸附判定升级为三轴角度比较（x/y/z 均 `< ROT_TOL=10°`）+ 平面距离 `< tolerance`；`targetRotation` 支持 z 轴倾斜（悬空寺 rz:15）。
  - 关卡切换 `loadLevel(i)` 严格：① 清除上一关 columnGroup + 所有 pieces（`disposeGroup`）② 按新数据重建柱/岩壁与构件 ③ 更新 `hud-left`(关卡名)/`hud-right`(操作提示)/`hud-prog` ④ 相机重置（`camCenter` 取目标均值、position 重置、dragPlane 高度随激活构件）。
- 验证：`node --check` app.js/data.js 通过；冒烟测试 `_smoke_build.js`（`vm` 加载 data.js+app.js，经真实控制器 `mount('build')`，点 next-btn 遍历 4 关含多构件与岩壁关 + dispose 重挂载）→ SMOKE_OK 无异常（临时文件已删）。
- 注意：本次仅改 `sceneBuild` 一段；闯关区/藏品馆/社区/我的 的逻辑与渲染函数未改动，符合「剩下不变」。

## 古建区再追加三关（L5–L7，其余不变！！！）

- 需求：在已重构的 `levels` 数组（数据驱动）**再追加三关**，其余四模块不动；每关切换须 ①清上一关动态物体 ②按新数据重建柱/斗拱 ③更新左上关卡名+右上提示 ④重置相机。
- 仅改 `app.js` 的 `levels` 数组（在 L4 后追加 3 条），复用既有 `loadLevel`/`createColumn`/`createPiece`/`evaluateSnap`，**未改任何函数体**，其余场景零改动。
- 新增关卡：
  5. 平遥民居院落·燕尾锁梁（燕尾榫梁枋，深褐柱 #5C3317，target{x:2,y:1.5,z:0,rz:2°}，tol 0.15，next「前往乔家大院」）
  6. 乔家大院·垂花倒挂隼（朱黄柱 #BFA748，sequential 四构件依次解锁，targets {0,1.1,0}/{0,2.0,0.9}/{0,2.9,0}/{0,3.8,0.9}，tol 0.2，next「前往南禅寺」）
  7. 南禅寺·唐构脊梁（短直榫横梁，黄土灰柱 #868068，target{x:-1.0,y:1.0,z:0,rz:15°倾斜}，tol 0.15，next「恭喜通关」）
- 现共 7 关（晋祠→佛光寺→应县→悬空寺→平遥→乔家→南禅寺），第 7 关按钮「恭喜通关」，点后 `(current+1)%7` 回到第 1 关。`hud-prog` 自动显示「第 N / 7 关」。
- 验证：`node --check` app.js/data.js 通过；冒烟测试 `_smoke_lv.js`（`vm` 加载 data.js+app.js，经 `mount('build')` 后点 next-btn 7 次遍历全部关卡）→ 7 关名全部正确、切换无异常（临时文件已删）。提示：L1 显示空字符串是 stub 时序问题，真实浏览器 loadLevel(0) 会正常写入「晋祠圣母殿」。

## 跨模块联动：闯关掉件 → 去拼古建 → 藏品/成就（用户三点要求）

- 需求：①只有闯关区答题成功才得榫卯零件、才能去古建区拼古建 ②每闯完一关底部有「去拼古建」按钮跳古建区 ③每拼好一座古建，完整样貌进藏品区、并在「我的」显示成就。其余模块逻辑不变。
- 改动（均为最小侵入）：
  - `data.js`：给 `BUILDINGS` 增 `jinci`（晋祠圣母殿）条目，使 7 个古建关都能在藏品馆以彩色模型出现（否则晋祠不显示）。
  - `app.js` 控制器：新增 `window.mount = mount;`，供各场景内联 `onclick="mount('build'/'challenge')"` 跳转。
  - `sceneChallenge`（`finishLevel`）：通关面板加「去拼古建 ▶」按钮 → `mount('build')`。（零件本就只在答对时写入 inventory，天然满足"答题才有零件"。）
  - `sceneBuild`：
    - 每关 `levels` 加 `unlock`（需在闯关区完成的 LEVELS id）与 `bid`（对应 BUILDINGS id）。映射：晋祠←basic、佛光寺←boss_foguang、应县←boss_muta、悬空寺←boss_xuankong、平遥←jinshang、乔家←boss_qiao、南禅寺←boss_nanchai。
    - `loadLevel` 计算 `locked = L.unlock && !state.clearedLevels[L.unlock]`：锁定则隐藏构件 + 显示 `#lock-msg` 覆盖层（"需在闯关区完成「X」后解锁"+「去闯关区答题解锁」按钮）；`onPointerDown` 首行 `if(locked) return;` 禁拖拽。→ 满足"答题成功获得零件才能拼对应古建"。
    - `showNextButton` 中调用 `recordBuilt(levels[current])`：拼好后把 `{id:L.bid,name:L.building}` 推入 `state.built` 并 `saveState()`。→ 藏品馆/我的自动反映。
  - `sceneProfile`：报告卡加「已解锁成就」列表 `#rp-ach`，列出 `state.built` 中的古建名。
  - `style.css`：新增 `#lock-msg/.lock-box/#lock-go` 与 `.ach-title/.ach-list/.ach` 样式。
- 验证：
  - `node --check` app.js/data.js 通过。
  - 数据一致性脚本：7 关 `unlock` 全在 LEVELS、`bid` 全在 BUILDINGS、`jinci` 已加入 → 通过 ✅。
  - 运行时冒烟（宽松 THREE/DOM 桩，`vm` 加载 data.js+app.js）：`window.mount` 可用；仅 basic 通关时 古建关1 晋祠未锁定(lock-msg=none)、点下一关到关2 佛光寺锁定(lock-msg=flex)；藏品馆/我的 挂载读存档无异常 → 全通过 ✅。
- 设计取舍：关1 晋祠也用 `unlock:'basic'` 严格门禁（开古建区首见可能全锁，锁屏明确提示去闯关），最贴合"必须答题才拼古建"。如想让首关免门槛可改 `unlock:null`。

## 修复：通关后"去拼古建"按钮不可见/不明显

- 现象：用户反馈"通关后没有'去拼古建'按钮"。经 `vm`+DOM 桩集成测试驱动 `mount('challenge')→startLevel→answer→nextQ` 全流程，确认 `finishLevel` 实际**已生成**该按钮（`<button onclick="mount('build')">去拼古建 ▶</button>`），源码无误。
- 真实原因判定：①UX 歧义——最后一题按钮仍写"下一题"，用户点完最后答案后没意识到要再点一次才进 通关屏；②file:// 双击场景下浏览器可能缓存旧 app.js，看不到最新改动。
- 改动（`app.js` 仅 `sceneChallenge`）：
  - `renderQuestion`：最后一题(next 按钮)文案改为「🏛️ 完成本关 · 去拼古建」（仍 `onclick="nextQ()"`→`finishLevel`）。
  - `finishLevel`：把「去拼古建」按钮从 `.row` 内第三个小按钮改为**整行 100% 宽绿色主按钮**（`onclick="window.mount('build')"`），明确且不可错过；"返回关卡/再答一次"保留在上方 `.row`。
- 验证：`node --check` 通过；重跑集成测试，通关面板 HTML 含整行 `去拼古建` 按钮 ✅。
- 已提醒用户若仍看不到，需硬刷新（Ctrl/Cmd+Shift+R）或重新双击打开 index.html 以绕过 file:// 缓存。

## 古建区重做为"电影感"拼装（拖榫卯→咬合动画→由中心向外逐层自动组装）

- 用户要求（唯一焦点）：把古建区交互改成——将闯关得来的专属榫卯拖拽到建筑中心指定咬合点位 → 触发榫卯咬合动画 → 整栋建筑由中心向外逐层自动组装成型。其余模块零改动。
- 仅改 `app.js` 的 `sceneBuild`（约 225–653 行）整段重写；`levels` 数组加 `part/foot/tiers/temple/roofColor` 字段；其余场景工厂未动。
- 新模型（单专属榫卯 + 程序化整栋古建）：
  - `buildFull(L)`：按 `foot/tiers/temple/rock/columnColor/roofColor` 参数化程序生成整栋古建，拆成多层 `layers`（台基→中心柱→周圈柱列→斗拱→梁枋→(逐层向上)→屋顶→脊饰→(岩壁)），初始 `visible=false` 且 `scale=0.02`（隐藏待组装）。
  - `createSnapMarker()`：场景中心（0, SNAP_Y=2.4）发光圆环 + 竖直光柱，`animate` 里呼吸闪烁，标记"指定咬合点位"。
  - `createPiece(type,colorHex)`：按本关 `tenon` 造型 + `PARTS[L.part].color` 染色的专属榫卯（颜色取自闯关掉落件，呼应"闯关得来的专属榫卯"）。
  - 交互：`onPointerDown/Move/Up` 把榫卯拖到水平面（固定高度 SNAP_Y）；`evaluateSnap` 以**落点位置为主**判定（距中心 `<SNAP_TOL=0.95` 即咬合，旋转仅作宽松校验 `<π/2`，因为咬合动画会把旋转归零飞向中心，无需用户精确摆正——放宽了原 20° 容差导致"只拖不转就失败"的坑）。
  - 咬合动画 `snapSuccess`：榫卯变青绿自发光 + Web Audio 咔嗒音 + `startTween` 飞向中心 (0,SNAP_Y,0) 且旋转归零。
  - 自动组装 `startAssembly/updateAssembly`：从 `layers[0]` 起每隔 `gap=0.22s` 显现一层并 `scale` 由 0.02 easeOut 弹大到 1（由中心台基/中心柱 → 向外周圈柱 → 斗拱 → 梁枋 → 屋顶 → 脊饰），"由中心向外逐层成型"；全部完成 `onAssembled` → 镜头环绕一周 + 显示"下一关"按钮 + `recordBuilt` 写存档（藏品馆/我的 同步）。
  - 解锁门禁保留：`locked` 时隐藏榫卯与咬合点位、显示 `#lock-msg`。`loadLevel` 严格清场重建 + 重置相机/拖拽平面/HUD。
  - 验证：
  - `node --check` app.js/data.js 通过。
  - 集成测试 `_test_build.js`（功能型 THREE 桩 + DOM 桩，`vm` 加载 data.js+app.js，手动 `mount('build')`，派发 pointerdown→move(到中心)→pointerup，泵 120 帧驱动 tween+assembly）：确认 ①L0 晋祠因 basic 已通关故未锁定(lock-msg=none) ②desc 含"发光咬合点位/由中心向外逐层" ③驱动后 `state.built` 含 `{id:'jinci'}` 且 `next-wrap` 显示 block → **全链路 PASS** ✅。临时文件已删。

## 微调：古建区榫卯吸附太难 → 放宽易拼

- 用户反馈"榫卯有点太难拼上去"。根因三处：①吸附容差偏小(SNAP_TOL=0.95) ②发光圆环画在地面(y=0.62)而吸附点在高空(y=2.4)，视觉错位总对不准 ③榫卯起点离中心太远(foot*0.55+1.6≈4.0)。
- 仅改 `app.js` 的 `sceneBuild`（其余模块零改动）：
  - `SNAP_TOL` 0.95 → **2.0**（落点半径放宽一倍多，中心附近松手即咬合）。
  - `createSnapMarker` 发光圆环 `ring.position.y` 0.62 → **SNAP_Y(2.4)**，与榫卯实际吸附点同高；环半径 0.55→0.7 更易读。消除视差，所见即所拼。
  - `onPointerMove` 加**磁吸**：距中心 < SNAP_TOL*1.6 时把落点向中心轻拉 40%(k=0.4)，手感"想吸上去"。
  - `loadLevel` 起点从远处改为固定近中心 `(2.6, SNAP_Y, 1.6)`，初始偏航 0.4→0.25。
  - `snapFail` 提示改为"再往中心发光圆环拖一点就好～"。
- 验证：`node --check` 通过；集成测试 `_test_easy.js`（拖到中心→泵帧）全链路仍 PASS ✅（放宽后不影响"拖到中心即咬合→逐层组装→写存档"）。临时文件已删。

## 微调：去掉拼接成功的弹窗提示文字

- 用户要求拼成榫卯后不显示"榫卯咬合成功，建筑物由内而外……"这类提示字。
- 仅删 `app.js` `sceneBuild` 的 `startAssembly()` 内 `showHint('ok', '榫卯咬合！整栋建筑由中心向外逐层组装成型…')` 一行（约原 523 行）。拼接后只播组装动画、不再弹文字；失败提示与"组装完成"确认提示保留。
- 验证：`node --check` 通过；grep 确认弹窗式即时提示已无残留（desc/关卡 description 中的"由中心向外"为静态说明文案，非弹窗，保留）。

## 微调：藏品馆已解锁建筑更亮更明显

- 用户要求在藏品馆让已解锁建筑"亮一点、明显一点"。仅改 `sceneMuseum`（`buildModel` 与台座绘制），未解锁保持灰暗半透明做对比：
  - 已解锁木/瓦材质加自发光（woodMat emissive 0x5a3010/.42、roofMat emissive=COLOR_ROOF/.28）并降低 roughness（.5）使其更亮反光。
  - 已解锁建筑脚下加金色发光光环（TorusGeometry(1.3) MeshBasicMaterial 0xE8B04B opacity .9）。
  - 已解锁台座加微发光（base emissive 0x3a2417/.3）。
- 验证：`node --check` 通过；grep 确认 688/689/698/726 四处改动生效。

## 修复：藏品馆"加载失败"（unlocked TDZ）

- 用户反馈藏品区显示"加载失败"。根因：上一步给台座加发光时，把 `unlocked` 用在材质三元表达式里（726 行），但 `const unlocked` 是在 728 行才声明——`const` 暂时性死区（TDZ）导致 `initScene` 执行到 726 行即抛 `ReferenceError: Cannot access 'unlocked' before initialization`，被 762 行 catch 捕获并显示"加载失败：…"，整个藏品馆白屏。
- 修复：把 `const unlocked = builtIds.has(B.id);` 提到 `list.forEach` 循环体开头（724 行后），726 行与 buildModel 调用共用它。
- 验证：写 `_test_museum.js`（功能型 THREE 桩 + DOM 桩，`vm` 加载 data.js+app.js，分别用空存档/全解锁存档 `mount('museum')`，各泵数帧）确认两分支 `loadingStub.textContent` 均为空、无"加载失败"抛错 → **双分支 PASS** ✅。临时文件已删。

## 新增：HTML 初始主菜单 menu.html

- 用户要求为《晋楹记》做独立 HTML 初始主菜单（动效 + 音效 + 角落信息）。新建 `menu.html`（自包含、离线可用、无外部依赖）：
  - 背景：深褐径向渐变 + 内联 SVG 应县木塔半透明线稿（缓慢浮动动画）+ 底部木纹台基色块（repeating-linear-gradient 木纹 + 金顶线）。
  - 标题「晋楹记」朱红书法字 + `text-shadow` 木刻凹凸（深褐立体阴影 + 金高光）；副标题米黄小字。
  - 配色变量 `--zhu #8B1A1A / --he #5C3317 / --mi #F5F5DC / --jin #FFD700`；字体优先楷体/宋体。
  - 动效：按钮加载时 `slideInLeft` 从左侧依次展开（animation-delay 按 `--i` 错峰）；点击 `press` 关键帧向下微移再回弹（绑定一次，非重复监听）。
  - 音效：Web Audio 程序化合成氛围音（风铃正弦衰减 / 鸟鸣频率滑音 / 木材吱呀锯齿+低通扫频），首次 `pointerdown` 解锁 AudioContext，右下鲁班锁按钮 `toggleAudio()` 开关，默认开启。
  - 角落：左上「山西 · 古建巡礼」、右上 SVG 环形伪进度 60%（`stroke-dasharray:98 65.4`）、右下鲁班锁图标按钮。
  - 按钮「开始游戏/古建拼装/藏品图鉴/我的成就」经 `location.href='index.html#xxx'` 进入游戏对应模块。
- 配套改动 `app.js` 的 `initApp`：新增 hash 路由——读取 `location.hash` 映射到 `challenge/build/museum/community/profile`，合法则直接 `mount`，并监听 `hashchange`；默认仍 `challenge`。使主菜单按钮能直达对应模块。
- 验证：`menu.html` 内联 JS 提取后 `node --check` 通过；`node --check app.js` 通过；桩测试 `_test_hash.js`（THREE 桩含 Plane/Euler/MathUtils/PointLight/setScalar）加载 data.js+app.js 且 `location.hash='#build'`，确认 `build` 初始化未抛错、未显示"加载失败" → PASS ✅。临时文件已删。

## 新增：知识卡弹窗（答题后 / 拼完古建后）

- 需求：每次答完题弹「相关题目知识卡」；每次拼完一座榫卯古建弹「该建筑知识卡」；点空白处关闭继续。
- 实现（app.js IIFE 顶层，`outColor` 之后）：共享函数 `window.showKnowledgeCard({tag,title,subtitle,body,points})`——懒加载单例遮罩 `#kcard-overlay`（appendChild 到 body），内含 `.kcard`（tag/title/sub/body/points/hint）。只在 `e.target===overlay`（点空白遮罩）时 `hideKCard`，点卡片本体不关闭。进入动画：display=flex→回流→加 `.show` 类。
- 触发点：①闯关 `window.answer(i)` 末尾——`tag=📖+关名, title=答对了!/正确答案在此, subtitle=q.q, body=正确答案+q.explain, points=答对时 PARTS[q.reward] 名称+desc`；②古建 `onAssembled()`——按 `levels[current].bid` 查 `BUILDINGS`，`tag=🏛️古建知识卡, title=B.name, subtitle=城市·地区·星级, body=B.doc, points=前4层 layer.popup(title:text)`；已移除原 `showHint('✅…组装完成')`（改由知识卡承载）。
- 样式（style.css 末尾）：`#kcard-overlay` fixed inset:0 z-index:999 半透明+blur；`.kcard` 宣纸米黄渐变+金边圆角，`.kcard-tag` 朱红胶囊，`.kcard-title` 书法朱红，`.kcard-hint` 呼吸闪烁「点击空白处继续」。
- 验证：`node --check app.js` 通过；`_test_kcard.js`（功能型 DOM 桩：innerHTML 正则提取 id 建子元素、querySelector 按 id 取；readyState='loading' 避免 initApp）确认弹出后 display=flex/show 类/各字段文本正确、点空白 target===overlay 移除 show、点卡片本体保留 show → PASS ✅。临时文件已删。

## 新增：全局背景音乐系统（BGM + 音效，分离增益 + 设置面板）

- 需求：为整个游戏加古典背景音乐，音量略大能清楚循环、自然融入不影响音效清晰度；提供音乐开关 + 设置里自由调节 BGM/音效音量。
- 选型与实现：项目全程离线自包含，**无音频文件**，背景曲用 Web Audio 程序合成古筝五声音阶循环曲《雅致茶音》（宫商角徵羽 G 调，SCALE 9 音 + MELODY 32 步 + 低音铺底 + 轻和声，BEAT=0.42s，lookahead 调度 setInterval 160ms）。
- 架构（app.js IIFE 顶层，ctx 之后）：全局单例 `AudioSys`（挂 `window.AudioSys`）：独立 `master/bgmGain/sfxGain` 三个 GainNode——BGM 与音效增益**完全分离**，互不影响清晰度；设置存 `localStorage['jinyingji_audio_v1']`（bgmOn/sfxOn/bgmVol/sfxVol，默认 0.6/0.85）。删除了原 sceneBuild 内本地 `audioCtx/resumeAudio/playClick`，改走 `AudioSys.resume()/playClick()`（避免重复 AudioContext）。
- 设置面板：index.html 顶部导航加「⚙ 设置」按钮 + `#settings-panel` 模态（背景音乐·雅致茶音 / 游戏音效 各含 开关toggle + range 滑块 + 百分比）；`setupAudioUI()` 初始化控件、开/关/调音量实时写回 AudioSys 并持久化；点空白遮罩或「关闭」关闭。style.css 新增 `.modal-mask/.settings-box/.toggle/range` 等，沿用朱红#9B1A1A+宣纸米黄+金#FFD700 风格。
- 自动播放：initApp 注册一次性 `pointerdown/keydown` 监听，首次用户交互时 `AudioSys.resume()` + 若 bgmOn 则 `startBgm()`（满足浏览器自动播放策略）。切场景不重启 BGM（全局运行）。
- 验证：`node --check app.js` 通过；`_test_audio.js`（功能型 AudioContext 桩 + DOM 桩，vm 合并 data.js+app.js 加载，readyState='complete' 触发 initApp 含 mount('challenge')+setupAudioUI 不抛错）确认 AudioSys 暴露、API 齐全、开关/音量/startBgm/playClick/首次 pointerdown 启动 BGM 全 OK → **ALL PASS** ✅。临时文件已删。

## 新增：防卡关「跳过」按钮（答题三次失败 / 拼装三次失败）

- 需求：防止用户卡关——答题三次失败后、拼装失败（累计 3 次未咬合）后自动显示「跳过」按钮，允许继续后续流程。配色统一：底 `#D2B48C`(木纹黄褐)、字 `#2E8B57`(青绿)。
- 仅改 `app.js` + `style.css`，其余模块零改动：
  - 样式 `style.css`：新增 `.btn.skip{background:#D2B48C;color:#2E8B57;border:2px solid #2E8B57}`（hover 微亮+上移）；新增 `#skip-build-btn`（古建区底部居中，同色系，默认 display:none）。
  - 闯关区 `sceneChallenge`：`renderQuestion` 的 `.row` 增 `#skipBtn`(inline-block 隐藏)；`window.answer(i)` 答错累计 `wrongCount`——前 2 次仅标红已选项、重新启用其余选项供重试（不揭示正确答案）；第 3 次揭示正确答案 + 显示 `#skipBtn` 与「下一题」；答对仍弹知识卡（答错重试不再弹卡，减少噪音）。新增 `window.skipQuestion()`：隐藏知识卡后 `nextQ`/末题 `finishLevel` 直接继续，不掉落奖励。
  - 古建区 `sceneBuild`：`loadLevel` 重置 `failCount=0` 并隐藏 `#skip-build-btn`；`snapFail` 累计 `failCount`，达 3 次显示 `#skip-build-btn`；`snapSuccess` 重置 `failCount` 并隐藏；新增 `skipBuild()`：标记 `piece.solved=true` 防误拖、隐藏榫卯与咬合点位、直接 `startAssembly()` 触发整栋自动组装→写存档→显示「下一关」+知识卡（即从"中心向外逐层"自动完成，不强制手拼）。绑定 `#skip-build-btn` 点击。
- 验证：`node --check app.js` 通过；功能型 THREE/DOM 桩冒烟测试（临时 `_test_skip.js`，已删）驱动 `mount('challenge')→startLevel→answer` 连续 3 次答错，断言 ①初始 skipBtn 隐藏 ②第1/2次后仍隐藏 ③第3次后 `skipBtn.style.display==='inline-block'` ④`skipQuestion()` 无异常；再 `mount('build')` 断言古建场景初始化无异常且 skip-build-btn 初始隐藏 → **ALL_PASS**，无 stderr 抛错。

## 新增：设置面板「清空游玩记录」（防误触二次确认）

- 需求（用户：「账号清空游玩记录」）：在游戏内提供一键清空进度（榫卯库存/木屑/已拼古建/已通关关卡/自由作品/签名等），即删除 localStorage 存档 `jinyingji_save_v1` 并重置。
- 实现（仅改 `index.html` + `style.css` + `app.js`，其余零改动）：
  - `index.html` `#settings-panel` 内新增一行 `.settings-danger`：`<button class="danger-btn" id="clear-save-btn">清空游玩记录</button>`。
  - `style.css`：`.danger-btn` 默认底 `#D2B48C`(木纹黄褐)/字 `#2E8B57`(青绿，与跳过按钮统一)；`.danger-btn.confirm` 警示态底 `#8B1A1A`(朱红)/字 `#F5F5DC`(米白)。
  - `app.js` `setupAudioUI()` 末尾绑定：采用**二次点击确认**——首次点击进入 `confirm` 警示态并 3s 自动复位（期间不清除，防误触）；确认态再次点击 → `localStorage.removeItem(SAVE_KEY)` 后 `location.reload()`（整体刷新界面、各场景重读空存档，最稳妥无残留；音频设置 `jinyingji_audio_v1` 不受影响）。
- 验证：`node --check app.js` 通过；功能型 THREE/DOM/AudioContext 桩冒烟测试（临时 `test_clear.js`，已删）确认 ①bundle 加载 + initApp（含 setupAudioUI）无异常（防"加载失败"）②`clear-save-btn` 已绑定 click ③首次点击后存档仍在且按钮进入 confirm 态 ④二次点击后存档被移除且触发 `location.reload` → **7 PASS，0 FAIL**。
